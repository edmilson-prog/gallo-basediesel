# PRD-042 Metas (Goals) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir o sistema de gestão de metas comerciais com 5 tipos, 3 escopos, 3 períodos, tracking em tempo real alimentado por pedidos pagos, dashboard duplo (vendedor individual + Gestor/Owner agregado), criação/edição/detalhe, gráfico evolutivo, status automático e notificações de marco.

**Architecture:** Feature isolada em `src/features/goals/` com engine de cálculo puro (`engine/calculate.ts`), hooks reativos (`useGoalProgress`, `useSellerGoals`, `useStoreGoals`), 3 páginas (lista, criação, detalhe), modal de edição, e widget de integração com PRD-014. O tipo `IGoal` existente em `src/shared/types/bi.ts` é estendido com campos do PRD-042 (`name`, `status`, `sellerId`, `rewardDescription`, `createdBy`); `GoalMetric` ampliado com `ticket_medio` e `novos_clientes`. Cálculo de progresso é runtime (memoizado por dependência) — `currentValue`/`progressPercent` persistidos no mock servem como cache desprezado pelos hooks novos.

**Tech Stack:** TypeScript strict + React 19 + TanStack Router (file-based) + TanStack Query + Recharts + shadcn/ui + Tailwind v4 + Iconify. Mocks via `@/providers/data` barrel (Provider Pattern). Sem suite de testes — validação via `bun run build` (tsc noEmit + Vite) e `bun run lint`.

---

## File Structure

**Modificar:**
- `src/shared/types/bi.ts` — estender `IGoal` com novos campos opcionais; ampliar `GoalMetric`.
- `src/shared/types/index.ts` — re-exportar novos tipos (`GoalStatus`, `IGoalProgress`, `GoalScope`, `GoalProgressStatus`).
- `src/mocks/generators/goal.ts` — popular novos campos + gerar mix de status/períodos (~25 metas).
- `src/routes/app.gestao.metas.tsx` — trocar `PlaceholderPage` por `<GoalsPage />`.
- `src/features/manager-dashboard/pages/ManagerDashboardPage.tsx` — adicionar widget "Metas do mês" na grid.
- `package.json` — bump v0.26.0.
- `CHANGELOG.md` — entrada v0.26.0 codinome "Pulse".
- `docs/prds/INDEX-PRDs-Gallo-Base-Diesel.md` — marcar PRD-042 ✅, atualizar contadores.
- `docs/prds/PRD-042-metas.md` → renomear para `PRD-042-metas_DONE.md`.

**Criar (`src/features/goals/`):**
- `pages/GoalsPage.tsx` — entry com renderização condicional por role.
- `pages/NewGoalPage.tsx` — formulário de criação com 4 seções.
- `pages/GoalDetailPage.tsx` — detalhe com 5 seções.
- `components/GoalCard.tsx` — card individual para dashboard do vendedor.
- `components/GoalProgressBar.tsx` — barra reutilizável com semáforo.
- `components/GoalStatusBadge.tsx` — badge de status visual.
- `components/GoalTypeBadge.tsx` — badge de tipo.
- `components/IndividualGoalsDashboard.tsx` — dashboard vendedor (cards).
- `components/AggregatedGoalsDashboard.tsx` — dashboard gestor/owner (KPIs + tabela + chart).
- `components/GoalsFiltersBar.tsx` — filtros URL-sincronizados.
- `components/GoalsTable.tsx` — tabela paginada.
- `components/GoalKpiRow.tsx` — 4 KPIs no topo do agregado.
- `components/EditGoalModal.tsx` — modal restrito (nome, target, recompensa).
- `components/CancelGoalDialog.tsx` — confirm com motivo.
- `components/SellerProgressBarChart.tsx` — bar chart Recharts por vendedor.
- `components/forms/GoalConfigSection.tsx` — Seção 1 (tipo, período, datas, nome).
- `components/forms/GoalScopeSection.tsx` — Seção 2 (escopo + vendedor/loja).
- `components/forms/GoalTargetSection.tsx` — Seção 3 (target + sugestões).
- `components/forms/GoalRewardSection.tsx` — Seção 4 (recompensa).
- `components/detail/GoalDetailHeader.tsx` — header (nome, badges, ações).
- `components/detail/GoalProgressSummary.tsx` — Seção 2 (barra grande, projeção).
- `components/detail/GoalEvolutionChart.tsx` — Seção 3 (LineChart realizado vs esperado).
- `components/detail/GoalCompositionSection.tsx` — Seção 4 (pedidos/clientes contribuintes).
- `components/detail/GoalHistorySection.tsx` — Seção 5 (audit log).
- `components/widget/GoalsWidget.tsx` — widget compacto p/ PRD-014.
- `hooks/useGoalsFilters.ts` — URL sync.
- `hooks/useGoalProgress.ts` — progress de uma meta única.
- `hooks/useGoalsWithProgress.ts` — lista de metas + progress (lista).
- `hooks/useSellerGoals.ts` — wrapper para consumo externo.
- `hooks/useStoreGoals.ts` — idem para loja.
- `hooks/useGoalsStatistics.ts` — KPIs agregados.
- `hooks/useGoalAutoStatusUpdate.ts` — roda no mount, transiciona metas vencidas.
- `hooks/useGoalMilestoneToast.ts` — dispara toast em 50/80/100%.
- `engine/calculate.ts` — funções puras de cálculo.
- `engine/projection.ts` — regressão linear + dias.
- `engine/suggestion.ts` — sugestão "mês anterior + 5%".
- `utils/labels.ts` — labels dos tipos/escopos/períodos.
- `utils/validation.ts` — validações de form.
- `utils/composition.ts` — resolve pedidos/clientes que compõem a meta.
- `i18n/pt-BR.ts` — strings UI.
- `index.ts` — barrel.

---

## Phase 1 — Modelo, mocks, engine, hooks

### Task 1.1: Estender tipo `IGoal` e adicionar tipos novos

**Files:**
- Modify: `src/shared/types/bi.ts`
- Create: `src/shared/types/goals.ts`
- Modify: `src/shared/types/index.ts`

- [ ] **Step 1: Ampliar `GoalMetric` e estender `IGoal` em `src/shared/types/bi.ts`**

Substituir o tipo `GoalMetric` e a interface `IGoal` por:

```typescript
/** Business metric tracked by a goal. */
export type GoalMetric =
  | "revenue"
  | "margin"
  | "tickets"
  | "ticket_medio"
  | "novos_clientes"
  | "positivacao"
  | "recovery"
  | "conversion";

/** Lifecycle state of a goal (PRD-042). */
export type GoalStatus = "ativa" | "concluida" | "arquivada" | "cancelada";

/**
 * Goal (meta) — target value for a metric, scoped by level and period.
 *
 * The `level` field maps to the PRD-042 concept of "scope": store/team/individual.
 *
 * @see ../../../docs/glossario.md#meta
 * @see ../../../docs/prds/PRD-042-metas_DONE.md
 */
export interface IGoal {
  id: ID;
  storeId: ID;
  level: GoalLevel;
  /** ID of the target entity (store, team or seller depending on `level`). */
  targetId: ID;
  /** Convenience copy of `targetId` when `level === "individual"` (PRD-042). */
  sellerId?: ID;
  period: IGoalPeriod;
  metric: GoalMetric;
  targetValue: number;
  /** Snapshot of progress at the last persistence — derived live via hooks (PRD-042). */
  currentValue: number;
  /** Computed progress (0..1+) — derived live via hooks. */
  progressPercent: number;
  /** Restricts the goal to a specific division when set. */
  division?: Division;
  /** Human-readable name (PRD-042). Autogenerated or customized. */
  name?: string;
  /** Lifecycle status (PRD-042). */
  status?: GoalStatus;
  /** Free-text reward description shown to the seller (PRD-042). */
  rewardDescription?: string;
  /** Actor (Owner/Gestor) that created the goal (PRD-042). */
  createdBy?: ID;
  /** Reason given when status moves to `cancelada` (PRD-042). */
  cancelReason?: string;
  createdAt: ISO8601;
  updatedAt: ISO8601;
}
```

- [ ] **Step 2: Criar `src/shared/types/goals.ts`**

```typescript
import type { ID } from "./common";

/** Runtime-computed status used by the traffic-light visual indicator. */
export type GoalProgressStatus = "no_caminho" | "atencao" | "atrasada" | "concluida";

/** Trend direction comparing current pace vs previous interval inside the same period. */
export type GoalProgressTrend = "subindo" | "estavel" | "caindo";

/**
 * Derived progress of a goal — computed in runtime by `calculateGoalProgress`.
 * Never persisted; consumers should call the hook each render.
 *
 * @see ../../../docs/prds/PRD-042-metas_DONE.md
 */
export interface IGoalProgress {
  goalId: ID;
  /** Current achieved value (BRL or count depending on metric). */
  currentValue: number;
  /** Percentage of `targetValue` reached (0..200). */
  percentage: number;
  /** Linear projection of where the goal will land if the pace holds. */
  projection: number;
  /** Days remaining until `endDate` (clamped at 0). */
  daysRemaining: number;
  /** Total days in the period (for "days ratio" comparisons). */
  totalDays: number;
  /** Traffic-light status. */
  status: GoalProgressStatus;
  /** Pace trend over the last interval vs the previous one. */
  trend: GoalProgressTrend;
  /** Pre-computed ratio `percentage / expectedAtDate` used by the UI. */
  paceRatio: number;
}
```

- [ ] **Step 3: Adicionar exports em `src/shared/types/index.ts`**

Adicionar à lista de re-exports:

```typescript
export type {
  IGoalProgress,
  GoalProgressStatus,
  GoalProgressTrend,
} from "./goals";
export type { GoalStatus } from "./bi";
```

- [ ] **Step 4: Validar build**

Run: `cd D:/claude/gallo-basediesel && bun run build 2>&1 | tail -5`
Expected: `✓ built in …` — type extensions backward-compatible.

- [ ] **Step 5: Commit**

```bash
git add src/shared/types/bi.ts src/shared/types/goals.ts src/shared/types/index.ts
git commit -m "feat(goals): extend IGoal with PRD-042 fields and add IGoalProgress"
```

---

### Task 1.2: Atualizar gerador de mocks de goals

**Files:**
- Modify: `src/mocks/generators/goal.ts`

- [ ] **Step 1: Reescrever gerador para popular novos campos e gerar mix de status**

Substituir o conteúdo de `src/mocks/generators/goal.ts` para:
- Gerar `name` autogenerado (formato: `"[Metric] [Periodo] [Mês/Ano]"`).
- Popular `sellerId` quando `level === "individual"`.
- Popular `status: "ativa"` no mês corrente.
- Popular `createdBy: "seller-marina-cardoso"` (Gestora) para individuais e `"seller-joao-gallo"` para store.
- Adicionar metas históricas dos últimos 6 meses com `status: "concluida"` ou `"arquivada"` conforme atingimento.
- Adicionar 2 metas com `status: "cancelada"` (com `cancelReason`).
- Variar metrics incluindo `ticket_medio` e `novos_clientes`.

Conteúdo completo:

```typescript
import type { IGoal, ISeller, ID, GoalMetric, GoalStatus } from "@/shared/types";
import { SEED_STORE_ID } from "../data";
import { monthRange, monthRef, type ISeededContext } from "./utils";

const METRIC_LABELS: Record<GoalMetric, string> = {
  revenue: "Faturamento",
  margin: "Margem",
  tickets: "Pedidos",
  ticket_medio: "Ticket médio",
  novos_clientes: "Novos clientes",
  positivacao: "Positivação",
  recovery: "Recuperação",
  conversion: "Conversão",
};

const PERIOD_LABEL = "mensal";

function autoName(metric: GoalMetric, date: Date): string {
  const month = date.toLocaleDateString("pt-BR", { month: "long" });
  const capitalized = month.charAt(0).toUpperCase() + month.slice(1);
  return `${METRIC_LABELS[metric]} ${PERIOD_LABEL} — ${capitalized} ${date.getFullYear()}`;
}

/**
 * Goal set (PRD-042): mix of active monthly + historical concluded/archived +
 * 2 canceled. Roughly ~25 goals when seeded with 4 active sellers + 6 months of
 * history. Team-level goals stay dormant.
 */
export function generateGoals(
  ctx: ISeededContext,
  options: { sellers: ISeller[]; now?: Date },
): IGoal[] {
  const now = options.now ?? new Date();
  const goals: IGoal[] = [];

  goals.push(...buildPeriod(ctx, now, options.sellers, "ativa"));

  for (let i = 1; i <= 5; i += 1) {
    const past = new Date(now.getFullYear(), now.getMonth() - i, 1);
    const targets = buildPeriod(ctx, past, options.sellers, undefined);
    for (const g of targets) {
      const reachedTarget = g.currentValue >= g.targetValue;
      g.status = reachedTarget ? "concluida" : "arquivada";
      goals.push(g);
    }
  }

  goals.push(
    cancelGoal(
      makeGoal(ctx, {
        id: `goal-${monthRef(now)}-store-recovery-cancelled`,
        level: "store",
        targetId: SEED_STORE_ID,
        sellerId: undefined,
        metric: "recovery",
        targetValue: 25,
        currentValue: 4,
        start: monthRange(now).start,
        end: monthRange(now).end,
        status: "cancelada",
      }),
      "Mudança de estratégia comercial para o trimestre",
    ),
  );

  return goals;
}

function buildPeriod(
  ctx: ISeededContext,
  date: Date,
  sellers: ISeller[],
  status: GoalStatus | undefined,
): IGoal[] {
  const out: IGoal[] = [];
  const { start, end } = monthRange(date);
  const period = monthRef(date);

  out.push(
    makeGoal(ctx, {
      id: `goal-${period}-store-revenue`,
      level: "store",
      targetId: SEED_STORE_ID,
      sellerId: undefined,
      metric: "revenue",
      targetValue: 1_200_000,
      currentValue: ctx.int(400_000, 1_400_000),
      start,
      end,
      status,
    }),
  );

  out.push(
    makeGoal(ctx, {
      id: `goal-${period}-store-positivacao`,
      level: "store",
      targetId: SEED_STORE_ID,
      sellerId: undefined,
      metric: "positivacao",
      targetValue: 70,
      currentValue: ctx.int(40, 90),
      start,
      end,
      status,
    }),
  );

  for (const seller of sellers) {
    if (seller.id === "seller-joao-gallo") continue;
    out.push(
      makeGoal(ctx, {
        id: `goal-${period}-${seller.id}-revenue`,
        level: "individual",
        targetId: seller.id,
        sellerId: seller.id,
        metric: "revenue",
        targetValue: 350_000,
        currentValue: ctx.int(80_000, 450_000),
        start,
        end,
        status,
      }),
    );
    out.push(
      makeGoal(ctx, {
        id: `goal-${period}-${seller.id}-tickets`,
        level: "individual",
        targetId: seller.id,
        sellerId: seller.id,
        metric: "tickets",
        targetValue: 25,
        currentValue: ctx.int(8, 32),
        start,
        end,
        status,
      }),
    );
  }

  if (sellers.length > 0) {
    const featured = sellers.find((s) => s.id !== "seller-joao-gallo") ?? sellers[0];
    out.push(
      makeGoal(ctx, {
        id: `goal-${period}-${featured.id}-ticket-medio`,
        level: "individual",
        targetId: featured.id,
        sellerId: featured.id,
        metric: "ticket_medio",
        targetValue: 1_000,
        currentValue: ctx.int(600, 1_300),
        start,
        end,
        status,
      }),
    );
    out.push(
      makeGoal(ctx, {
        id: `goal-${period}-${featured.id}-novos`,
        level: "individual",
        targetId: featured.id,
        sellerId: featured.id,
        metric: "novos_clientes",
        targetValue: 10,
        currentValue: ctx.int(1, 14),
        start,
        end,
        status,
      }),
    );
  }

  return out;
}

interface IMakeGoalInput {
  id: ID;
  level: IGoal["level"];
  targetId: ID;
  sellerId: ID | undefined;
  metric: GoalMetric;
  targetValue: number;
  currentValue: number;
  start: Date;
  end: Date;
  status: GoalStatus | undefined;
}

function makeGoal(_ctx: ISeededContext, input: IMakeGoalInput): IGoal {
  const progressPercent = input.targetValue > 0 ? input.currentValue / input.targetValue : 0;
  const nowISO = new Date().toISOString();
  return {
    id: input.id,
    storeId: SEED_STORE_ID,
    level: input.level,
    targetId: input.targetId,
    sellerId: input.sellerId,
    period: {
      type: "monthly",
      start: input.start.toISOString(),
      end: input.end.toISOString(),
    },
    metric: input.metric,
    targetValue: input.targetValue,
    currentValue: input.currentValue,
    progressPercent,
    division: "parts",
    name: autoName(input.metric, input.start),
    status: input.status ?? "ativa",
    createdBy: input.level === "store" ? "seller-joao-gallo" : "seller-marina-cardoso",
    createdAt: nowISO,
    updatedAt: nowISO,
  };
}

function cancelGoal(goal: IGoal, reason: string): IGoal {
  goal.status = "cancelada";
  goal.cancelReason = reason;
  return goal;
}
```

- [ ] **Step 2: Validar build**

Run: `cd D:/claude/gallo-basediesel && bun run build 2>&1 | tail -5`
Expected: `✓ built in …`.

- [ ] **Step 3: Commit**

```bash
git add src/mocks/generators/goal.ts
git commit -m "feat(goals): seed historical and canceled goals with PRD-042 fields"
```

---

### Task 1.3: Engine de cálculo de progresso

**Files:**
- Create: `src/features/goals/engine/calculate.ts`
- Create: `src/features/goals/engine/projection.ts`
- Create: `src/features/goals/utils/labels.ts`

- [ ] **Step 1: Criar `src/features/goals/utils/labels.ts`**

```typescript
import type { GoalMetric } from "@/shared/types";

export const GOAL_METRIC_LABEL: Record<GoalMetric, string> = {
  revenue: "Faturamento",
  margin: "Margem",
  tickets: "Quantidade de pedidos",
  ticket_medio: "Ticket médio",
  novos_clientes: "Novos clientes",
  positivacao: "Positivação",
  recovery: "Recuperação",
  conversion: "Conversão",
};

export const GOAL_METRIC_SHORT_LABEL: Record<GoalMetric, string> = {
  revenue: "Faturamento",
  margin: "Margem",
  tickets: "Pedidos",
  ticket_medio: "Ticket médio",
  novos_clientes: "Novos",
  positivacao: "Positivação",
  recovery: "Recuperação",
  conversion: "Conversão",
};

export const GOAL_METRIC_ICON: Record<GoalMetric, string> = {
  revenue: "mdi:cash-multiple",
  margin: "mdi:chart-pie",
  tickets: "mdi:package-variant-closed",
  ticket_medio: "mdi:cart-outline",
  novos_clientes: "mdi:account-plus-outline",
  positivacao: "mdi:account-check-outline",
  recovery: "mdi:account-reactivate-outline",
  conversion: "mdi:swap-horizontal",
};

export const GOAL_METRIC_FORMAT: Record<GoalMetric, "currency" | "count" | "percent"> = {
  revenue: "currency",
  margin: "currency",
  tickets: "count",
  ticket_medio: "currency",
  novos_clientes: "count",
  positivacao: "count",
  recovery: "count",
  conversion: "percent",
};

export const GOAL_LEVEL_LABEL = {
  store: "Loja",
  individual: "Vendedor",
  team: "Equipe",
} as const;

export const GOAL_STATUS_LABEL = {
  ativa: "Ativa",
  concluida: "Concluída",
  arquivada: "Arquivada",
  cancelada: "Cancelada",
} as const;

export const GOAL_PROGRESS_STATUS_LABEL = {
  no_caminho: "No caminho",
  atencao: "Atenção",
  atrasada: "Atrasada",
  concluida: "Concluída",
} as const;
```

- [ ] **Step 2: Criar `src/features/goals/engine/projection.ts`**

```typescript
import type { IGoalPeriod } from "@/shared/types";

const DAY_MS = 24 * 3600_000;

export interface IPeriodWindow {
  totalDays: number;
  daysPassed: number;
  daysRemaining: number;
  daysRatio: number;
}

/**
 * Slice the period into total/passed/remaining day counts.
 * `now` defaults to wall-clock; injectable for testability.
 */
export function describePeriodWindow(period: IGoalPeriod, now: Date = new Date()): IPeriodWindow {
  const start = new Date(period.start).getTime();
  const end = new Date(period.end).getTime();
  const totalDays = Math.max(1, Math.round((end - start) / DAY_MS));
  const elapsedMs = Math.max(0, now.getTime() - start);
  const daysPassed = Math.max(0, Math.min(totalDays, Math.round(elapsedMs / DAY_MS)));
  const daysRemaining = Math.max(0, totalDays - daysPassed);
  const daysRatio = totalDays > 0 ? daysPassed / totalDays : 0;
  return { totalDays, daysPassed, daysRemaining, daysRatio };
}

/**
 * Linear projection of the final value if the current pace holds.
 * Capped at 200% of target so absurd extrapolations don't dominate charts.
 */
export function computeProjection(
  currentValue: number,
  daysPassed: number,
  totalDays: number,
  targetValue: number,
): number {
  if (daysPassed <= 0) return currentValue;
  const projected = currentValue * (totalDays / daysPassed);
  const cap = targetValue * 2;
  return Math.min(projected, cap);
}
```

- [ ] **Step 3: Criar `src/features/goals/engine/calculate.ts`**

```typescript
import type {
  ICustomer,
  IGoal,
  IGoalProgress,
  IOrder,
  GoalProgressStatus,
  GoalProgressTrend,
} from "@/shared/types";
import { computeProjection, describePeriodWindow } from "./projection";

export interface IGoalContext {
  orders: IOrder[];
  customers: ICustomer[];
  /** Reference clock — defaults to `new Date()`; injectable for memoization stability. */
  now?: Date;
}

function isWithin(iso: string | undefined, fromIso: string, toIso: string): boolean {
  if (!iso) return false;
  return iso >= fromIso && iso <= toIso;
}

function matchesGoal(order: IOrder, goal: IGoal): boolean {
  if (order.storeId !== goal.storeId) return false;
  if (goal.level === "individual" && order.sellerId !== goal.targetId) return false;
  if (goal.division && order.division !== goal.division) return false;
  return true;
}

function isPaid(order: IOrder): boolean {
  return order.paymentStatus === "pago";
}

function statusFromRatio(percentage: number, daysRatio: number): GoalProgressStatus {
  if (percentage >= 100) return "concluida";
  const expected = daysRatio * 100;
  if (expected <= 0) return "no_caminho";
  const ratio = percentage / expected;
  if (ratio >= 1.0) return "no_caminho";
  if (ratio >= 0.7) return "atencao";
  return "atrasada";
}

function computeTrend(
  goal: IGoal,
  orders: IOrder[],
  fromIso: string,
  toIso: string,
  now: Date,
): GoalProgressTrend {
  const half = new Date((new Date(fromIso).getTime() + now.getTime()) / 2).toISOString();
  let firstHalf = 0;
  let secondHalf = 0;
  for (const order of orders) {
    if (!matchesGoal(order, goal)) continue;
    if (!isPaid(order)) continue;
    const ts = order.paidAt ?? order.createdAt;
    if (!isWithin(ts, fromIso, toIso)) continue;
    if (ts < half) firstHalf += order.total;
    else secondHalf += order.total;
  }
  if (firstHalf === 0 && secondHalf === 0) return "estavel";
  if (firstHalf === 0) return "subindo";
  const diff = (secondHalf - firstHalf) / firstHalf;
  if (diff > 0.1) return "subindo";
  if (diff < -0.1) return "caindo";
  return "estavel";
}

/**
 * Pure function — compute the runtime progress of a goal given the orders and
 * customers in scope. Never reads `goal.currentValue` / `progressPercent`
 * (those are stale snapshots from the mock seed).
 */
export function calculateGoalProgress(goal: IGoal, context: IGoalContext): IGoalProgress {
  const now = context.now ?? new Date();
  const fromIso = goal.period.start;
  const toIso = goal.period.end;

  let currentValue = 0;

  switch (goal.metric) {
    case "revenue": {
      for (const o of context.orders) {
        if (!matchesGoal(o, goal)) continue;
        if (!isPaid(o)) continue;
        if (!isWithin(o.paidAt ?? o.createdAt, fromIso, toIso)) continue;
        currentValue += o.total;
      }
      break;
    }
    case "margin": {
      for (const o of context.orders) {
        if (!matchesGoal(o, goal)) continue;
        if (!isPaid(o)) continue;
        if (!isWithin(o.paidAt ?? o.createdAt, fromIso, toIso)) continue;
        for (const item of o.items) currentValue += item.marginValue;
      }
      break;
    }
    case "ticket_medio": {
      const matching: number[] = [];
      for (const o of context.orders) {
        if (!matchesGoal(o, goal)) continue;
        if (!isPaid(o)) continue;
        if (!isWithin(o.paidAt ?? o.createdAt, fromIso, toIso)) continue;
        matching.push(o.total);
      }
      currentValue =
        matching.length > 0 ? matching.reduce((a, b) => a + b, 0) / matching.length : 0;
      break;
    }
    case "tickets": {
      for (const o of context.orders) {
        if (!matchesGoal(o, goal)) continue;
        if (!isPaid(o)) continue;
        if (!isWithin(o.paidAt ?? o.createdAt, fromIso, toIso)) continue;
        currentValue += 1;
      }
      break;
    }
    case "positivacao": {
      const seen = new Set<string>();
      for (const o of context.orders) {
        if (!matchesGoal(o, goal)) continue;
        if (!isPaid(o)) continue;
        if (!isWithin(o.paidAt ?? o.createdAt, fromIso, toIso)) continue;
        seen.add(o.customerId);
      }
      currentValue = seen.size;
      break;
    }
    case "novos_clientes": {
      for (const c of context.customers) {
        if (c.storeId !== goal.storeId) continue;
        if (goal.level === "individual" && c.sellerId !== goal.targetId) continue;
        if (!isWithin(c.createdAt, fromIso, toIso)) continue;
        currentValue += 1;
      }
      break;
    }
    case "recovery":
    case "conversion": {
      // Métricas avançadas dependem de PRDs 044/053 — usar snapshot do mock.
      currentValue = goal.currentValue;
      break;
    }
  }

  const window = describePeriodWindow(goal.period, now);
  const percentage =
    goal.targetValue > 0 ? Math.round((currentValue / goal.targetValue) * 1000) / 10 : 0;
  const projection = computeProjection(
    currentValue,
    window.daysPassed,
    window.totalDays,
    goal.targetValue,
  );
  const paceRatio = window.daysRatio > 0 ? percentage / (window.daysRatio * 100) : 1;
  const status = statusFromRatio(percentage, window.daysRatio);
  const trend = computeTrend(goal, context.orders, fromIso, toIso, now);

  return {
    goalId: goal.id,
    currentValue,
    percentage,
    projection,
    daysRemaining: window.daysRemaining,
    totalDays: window.totalDays,
    status,
    trend,
    paceRatio,
  };
}
```

- [ ] **Step 4: Validar build**

Run: `cd D:/claude/gallo-basediesel && bun run build 2>&1 | tail -5`
Expected: `✓ built in …`.

- [ ] **Step 5: Commit**

```bash
git add src/features/goals/
git commit -m "feat(goals): add pure progress engine with 5 metric types"
```

---

### Task 1.4: Hooks reativos

**Files:**
- Create: `src/features/goals/hooks/useGoalsWithProgress.ts`
- Create: `src/features/goals/hooks/useGoalProgress.ts`
- Create: `src/features/goals/hooks/useSellerGoals.ts`
- Create: `src/features/goals/hooks/useStoreGoals.ts`
- Create: `src/features/goals/hooks/useGoalsStatistics.ts`

- [ ] **Step 1: Criar `src/features/goals/hooks/useGoalsWithProgress.ts`**

Hook agregador — único `useQuery` paralelo para goals + orders + customers; calcula progress de cada meta uma vez.

```typescript
import { useMemo } from "react";
import { useQueries } from "@tanstack/react-query";
import type { ID, IGoal, IGoalProgress } from "@/shared/types";
import {
  useCustomersProvider,
  useGoalsProvider,
  useOrdersProvider,
} from "@/providers/data";
import { calculateGoalProgress } from "../engine/calculate";

export interface IUseGoalsWithProgressParams {
  storeId?: ID;
  /** When set, only the goals matching this seller (`targetId` or `sellerId`). */
  sellerId?: ID;
  /** When set, restrict the returned goals to these lifecycle statuses. */
  statuses?: ("ativa" | "concluida" | "arquivada" | "cancelada")[];
}

export interface IGoalWithProgress {
  goal: IGoal;
  progress: IGoalProgress;
}

export interface IUseGoalsWithProgressResult {
  items: IGoalWithProgress[];
  isLoading: boolean;
  hasError: boolean;
  refetch: () => void;
}

const STALE_MS = 30_000;

export function useGoalsWithProgress(
  params: IUseGoalsWithProgressParams = {},
): IUseGoalsWithProgressResult {
  const goalsProvider = useGoalsProvider();
  const ordersProvider = useOrdersProvider();
  const customersProvider = useCustomersProvider();

  const queries = useQueries({
    queries: [
      {
        queryKey: ["goals", "list", params.storeId, params.sellerId],
        queryFn: () =>
          goalsProvider.list({
            storeId: params.storeId,
            targetId: params.sellerId,
            pageSize: 500,
          }),
        staleTime: STALE_MS,
      },
      {
        queryKey: ["goals", "orders", params.storeId, params.sellerId],
        queryFn: () =>
          ordersProvider.list({
            storeId: params.storeId,
            sellerId: params.sellerId,
            paymentStatus: "pago",
            pageSize: 2000,
          }),
        staleTime: STALE_MS,
      },
      {
        queryKey: ["goals", "customers", params.storeId],
        queryFn: () => customersProvider.list({ storeId: params.storeId, pageSize: 2000 }),
        staleTime: STALE_MS,
      },
    ],
  });

  const [goalsQuery, ordersQuery, customersQuery] = queries;
  const isLoading = queries.some((q) => q.isLoading);
  const hasError = queries.some((q) => q.isError);

  const items = useMemo<IGoalWithProgress[]>(() => {
    const goals = goalsQuery.data?.items ?? [];
    const orders = ordersQuery.data?.items ?? [];
    const customers = customersQuery.data?.items ?? [];

    const filtered = goals.filter((goal) => {
      if (params.sellerId && goal.level === "individual" && goal.targetId !== params.sellerId)
        return false;
      if (params.sellerId && goal.level !== "individual") return false;
      if (params.statuses && params.statuses.length > 0) {
        const status = goal.status ?? "ativa";
        if (!params.statuses.includes(status)) return false;
      }
      return true;
    });

    return filtered.map((goal) => ({
      goal,
      progress: calculateGoalProgress(goal, { orders, customers }),
    }));
  }, [
    goalsQuery.data,
    ordersQuery.data,
    customersQuery.data,
    params.sellerId,
    params.statuses,
  ]);

  return {
    items,
    isLoading,
    hasError,
    refetch: () => {
      void goalsQuery.refetch();
      void ordersQuery.refetch();
      void customersQuery.refetch();
    },
  };
}
```

- [ ] **Step 2: Criar `src/features/goals/hooks/useGoalProgress.ts`**

Hook para uma meta única — usa `useGoalsWithProgress` internamente.

```typescript
import { useMemo } from "react";
import { useQuery } from "@tanstack/react-query";
import type { ID, IGoal, IGoalProgress } from "@/shared/types";
import {
  useCustomersProvider,
  useGoalsProvider,
  useOrdersProvider,
} from "@/providers/data";
import { calculateGoalProgress } from "../engine/calculate";

export interface IUseGoalProgressResult {
  goal: IGoal | undefined;
  progress: IGoalProgress | undefined;
  isLoading: boolean;
  hasError: boolean;
  refetch: () => void;
}

const STALE_MS = 30_000;

export function useGoalProgress(goalId: ID | undefined): IUseGoalProgressResult {
  const goalsProvider = useGoalsProvider();
  const ordersProvider = useOrdersProvider();
  const customersProvider = useCustomersProvider();

  const goalsQuery = useQuery({
    queryKey: ["goals", "list", "all"],
    queryFn: () => goalsProvider.list({ pageSize: 500 }),
    staleTime: STALE_MS,
    enabled: Boolean(goalId),
  });

  const goal = goalsQuery.data?.items.find((g) => g.id === goalId);

  const ordersQuery = useQuery({
    queryKey: ["goals", "progress-orders", goal?.storeId, goal?.targetId],
    queryFn: () =>
      ordersProvider.list({
        storeId: goal?.storeId,
        sellerId: goal?.level === "individual" ? goal.targetId : undefined,
        paymentStatus: "pago",
        pageSize: 2000,
      }),
    staleTime: STALE_MS,
    enabled: Boolean(goal),
  });

  const customersQuery = useQuery({
    queryKey: ["goals", "progress-customers", goal?.storeId],
    queryFn: () => customersProvider.list({ storeId: goal?.storeId, pageSize: 2000 }),
    staleTime: STALE_MS,
    enabled: Boolean(goal),
  });

  const progress = useMemo(() => {
    if (!goal) return undefined;
    return calculateGoalProgress(goal, {
      orders: ordersQuery.data?.items ?? [],
      customers: customersQuery.data?.items ?? [],
    });
  }, [goal, ordersQuery.data, customersQuery.data]);

  return {
    goal,
    progress,
    isLoading: goalsQuery.isLoading || ordersQuery.isLoading || customersQuery.isLoading,
    hasError: goalsQuery.isError || ordersQuery.isError || customersQuery.isError,
    refetch: () => {
      void goalsQuery.refetch();
      void ordersQuery.refetch();
      void customersQuery.refetch();
    },
  };
}
```

- [ ] **Step 3: Criar `useSellerGoals.ts`, `useStoreGoals.ts`, `useGoalsStatistics.ts`**

```typescript
// useSellerGoals.ts
import type { ID } from "@/shared/types";
import { useGoalsWithProgress, type IUseGoalsWithProgressResult } from "./useGoalsWithProgress";

export function useSellerGoals(sellerId: ID | undefined): IUseGoalsWithProgressResult {
  return useGoalsWithProgress({
    sellerId,
    statuses: ["ativa", "concluida"],
  });
}
```

```typescript
// useStoreGoals.ts
import type { ID } from "@/shared/types";
import { useGoalsWithProgress, type IUseGoalsWithProgressResult } from "./useGoalsWithProgress";

export function useStoreGoals(storeId: ID | undefined): IUseGoalsWithProgressResult {
  return useGoalsWithProgress({
    storeId,
    statuses: ["ativa"],
  });
}
```

```typescript
// useGoalsStatistics.ts
import { useMemo } from "react";
import type { ID } from "@/shared/types";
import { useGoalsWithProgress } from "./useGoalsWithProgress";

export interface IGoalsStatistics {
  totalActive: number;
  averagePercentage: number;
  heroesCount: number;
  attentionCount: number;
  isLoading: boolean;
}

export function useGoalsStatistics(filters: { storeId?: ID; sellerId?: ID }): IGoalsStatistics {
  const { items, isLoading } = useGoalsWithProgress({
    storeId: filters.storeId,
    sellerId: filters.sellerId,
    statuses: ["ativa"],
  });

  return useMemo<IGoalsStatistics>(() => {
    const totalActive = items.length;
    const averagePercentage =
      items.length > 0
        ? Math.round(items.reduce((acc, i) => acc + i.progress.percentage, 0) / items.length)
        : 0;
    const heroesCount = items.filter((i) => i.progress.percentage >= 100).length;
    const attentionCount = items.filter(
      (i) => i.progress.percentage < 70 && i.progress.status !== "concluida",
    ).length;
    return { totalActive, averagePercentage, heroesCount, attentionCount, isLoading };
  }, [items, isLoading]);
}
```

- [ ] **Step 4: Validar build**

Run: `cd D:/claude/gallo-basediesel && bun run build 2>&1 | tail -5`
Expected: `✓ built in …`.

- [ ] **Step 5: Commit**

```bash
git add src/features/goals/hooks/
git commit -m "feat(goals): add reactive hooks for goal progress and statistics"
```

---

## Phase 2 — Dashboards e listagem

### Task 2.1: i18n strings + filtros URL-sincronizados

**Files:**
- Create: `src/features/goals/i18n/pt-BR.ts`
- Create: `src/features/goals/hooks/useGoalsFilters.ts`

- [ ] **Step 1: Criar `src/features/goals/i18n/pt-BR.ts`**

Strings completas (page title, tabs, labels de form, badges, mensagens vazias, validação, etc.). Listar pelo menos: `pageTitle`, `pageSubtitle`, `tabActive`, `tabHistory`, `kpiTotalActive`, `kpiAveragePct`, `kpiHeroes`, `kpiAttention`, `emptyVendedor`, `emptyGestor`, `createCta`, `editCta`, `archiveCta`, `cancelCta`, `tableColName`, `tableColType`, `tableColScope`, `tableColPeriod`, `tableColTarget`, `tableColProgress`, `tableColStatus`, `tableColActions`, `formConfigTitle`, `formScopeTitle`, `formTargetTitle`, `formRewardTitle`, `targetSuggestionLabel`, `validationEndBeforeStart`, `validationDuplicate`, `confirmCancel`, `cancelReasonRequired`, `targetChangeWarning`, `milestone50`, `milestone80`, `milestone100`.

- [ ] **Step 2: Criar `src/features/goals/hooks/useGoalsFilters.ts`**

URL sync com search params: `aba` (tab), `tipo` (metric), `escopo` (level), `status`, `vendedor` (targetId), `loja` (storeId), `periodo`. Inclui `validateGoalsSearch` para `validateSearch` da rota. Padrão idêntico a `useSalesFilters` do PRD-041 — adaptar campos.

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/i18n/ src/features/goals/hooks/useGoalsFilters.ts
git commit -m "feat(goals): add i18n strings and URL-synced filter state"
```

---

### Task 2.2: Componentes visuais reutilizáveis (badges, progress, card)

**Files:**
- Create: `src/features/goals/components/GoalStatusBadge.tsx`
- Create: `src/features/goals/components/GoalTypeBadge.tsx`
- Create: `src/features/goals/components/GoalProgressBar.tsx`
- Create: `src/features/goals/components/GoalCard.tsx`

- [ ] **Step 1: Criar `GoalStatusBadge.tsx`**

Recebe `progress.status` (no_caminho/atencao/atrasada/concluida) ou `goal.status` (ativa/concluida/arquivada/cancelada) via prop `mode: "progress" | "lifecycle"`. Renderiza badge colorido com label + icon do mapping em `utils/labels.ts`.

- [ ] **Step 2: Criar `GoalTypeBadge.tsx`**

Badge com icon + label do metric. Tamanhos `sm | md`.

- [ ] **Step 3: Criar `GoalProgressBar.tsx`**

Barra de progresso com cor semáforo. Props: `percentage`, `status`, `size: "sm" | "md" | "lg"`, `showLabel?`. Cor:
- `no_caminho` → verde (`bg-emerald-500`).
- `atencao` → amarelo (`bg-amber-500`).
- `atrasada` → vermelho (`bg-red-500`).
- `concluida` → verde brilhante com check.

Limita visual em 100% mas overlay mostra "+%" quando ultrapassa.

- [ ] **Step 4: Criar `GoalCard.tsx`**

Card individual (uso no dashboard do vendedor). Props: `goal`, `progress`, `onOpenDetail`. Renderiza:
- header: nome (truncate) + `<GoalTypeBadge />` + `<GoalStatusBadge mode="progress" />`.
- valor: `currentValue / targetValue` formatado (currency/count via `GOAL_METRIC_FORMAT`).
- `<GoalProgressBar />`.
- `daysRemaining` com label "dias restantes" + projeção compacta.
- recompensa (se houver).
- botão "Ver detalhes" → navega para `/app/gestao/metas/$id`.

- [ ] **Step 5: Validar build + commit**

```bash
git add src/features/goals/components/
git commit -m "feat(goals): add reusable progress, badge and card components"
```

---

### Task 2.3: Dashboard individual (vendedor)

**Files:**
- Create: `src/features/goals/components/IndividualGoalsDashboard.tsx`

- [ ] **Step 1: Criar `IndividualGoalsDashboard.tsx`**

Recebe `sellerId`, usa `useSellerGoals(sellerId)`. Renderiza:
- Header com saudação ("Olá, [nome]") + contador "Você tem N metas ativas".
- Grid responsiva (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) de `<GoalCard />`.
- `EmptyState` quando vazio: "Você ainda não tem metas ativas — fale com seu gestor".
- Aba secundária com histórico (status=concluida/arquivada) — `useGoalsWithProgress({ sellerId, statuses: ["concluida", "arquivada"] })`.

- [ ] **Step 2: Validar build + commit**

```bash
git add src/features/goals/components/IndividualGoalsDashboard.tsx
git commit -m "feat(goals): add individual goals dashboard for sellers"
```

---

### Task 2.4: Dashboard agregado (Gestor/Owner)

**Files:**
- Create: `src/features/goals/components/GoalKpiRow.tsx`
- Create: `src/features/goals/components/GoalsFiltersBar.tsx`
- Create: `src/features/goals/components/GoalsTable.tsx`
- Create: `src/features/goals/components/SellerProgressBarChart.tsx`
- Create: `src/features/goals/components/AggregatedGoalsDashboard.tsx`

- [ ] **Step 1: `GoalKpiRow.tsx`**

4 KPIs reaproveitando `KpiCard` do `manager-dashboard` (já usado no PRD-041):
- Total metas ativas.
- % média de atingimento.
- Vendedores ≥ 100% (heroes).
- Vendedores < 70% (atenção).

- [ ] **Step 2: `GoalsFiltersBar.tsx`**

Dropdowns (mesmo padrão do PRD-041 SalesHeader):
- Tipo (metric).
- Escopo (level).
- Status (lifecycle).
- Vendedor (lista via `useSellersProvider`).
- Loja (Owner only — Gestor com store travada).
- Período (mensal/trimestral/anual).

- [ ] **Step 3: `GoalsTable.tsx`**

Tabela com colunas (RF-013): Nome, Tipo, Escopo, Período, Datas, Target, Progresso (barra inline + valor + %), Status, Projeção, Ações (visualizar, editar, arquivar via menu). Linhas clicáveis → detalhe.

- [ ] **Step 4: `SellerProgressBarChart.tsx`**

Bar chart Recharts (vertical) — eixo X: vendedor, eixo Y: % atingido. Reaproveita padrão dos charts do PRD-041 (CSS tokens, tooltip, etc.). Filtra apenas metas individuais ativas.

- [ ] **Step 5: `AggregatedGoalsDashboard.tsx`**

Composição:
- `<GoalKpiRow />`.
- `<GoalsFiltersBar />` + botão "Nova meta" → `/app/gestao/metas/nova`.
- `<SellerProgressBarChart />`.
- `<GoalsTable />`.

Tabs: "Ativas" e "Histórico" (concluídas + arquivadas + canceladas).

- [ ] **Step 6: Validar build + commit**

```bash
git add src/features/goals/components/
git commit -m "feat(goals): add aggregated dashboard for managers and owner"
```

---

### Task 2.5: `GoalsPage` + atualizar rota

**Files:**
- Create: `src/features/goals/pages/GoalsPage.tsx`
- Create: `src/features/goals/index.ts`
- Modify: `src/routes/app.gestao.metas.tsx`
- Modify: `src/features/shell/config/navigation.ts` (ampliar roles).

- [ ] **Step 1: `GoalsPage.tsx`**

Decide qual dashboard renderizar por role:
- `Vendedor` → `<IndividualGoalsDashboard sellerId={currentUser.sellerId} />`.
- `Gestor` / `Owner` / `Financeiro` → `<AggregatedGoalsDashboard />` (com `gestorLockedStoreId` quando Gestor).
- Outros → `EmptyState` de acesso restrito.

Wrap em `DashboardLayout`.

- [ ] **Step 2: `src/features/goals/index.ts`** — barrel.

```typescript
export { GoalsPage } from "./pages/GoalsPage";
export { useSellerGoals } from "./hooks/useSellerGoals";
export { useStoreGoals } from "./hooks/useStoreGoals";
export { useGoalProgress } from "./hooks/useGoalProgress";
export { useGoalsStatistics } from "./hooks/useGoalsStatistics";
export { calculateGoalProgress } from "./engine/calculate";
export { validateGoalsSearch } from "./hooks/useGoalsFilters";
export { GoalsWidget } from "./components/widget/GoalsWidget"; // criado na Phase 5
```

- [ ] **Step 3: Atualizar rota `src/routes/app.gestao.metas.tsx`**

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { requireAuth } from "@/features/auth/guards";
import { GoalsPage, validateGoalsSearch } from "@/features/goals";

export const Route = createFileRoute("/app/gestao/metas")({
  beforeLoad: ({ location }) =>
    requireAuth(location.pathname, ["Owner", "Gestor", "Vendedor", "Financeiro"]),
  validateSearch: validateGoalsSearch,
  component: GoalsPage,
});
```

- [ ] **Step 4: Atualizar `src/features/shell/config/navigation.ts:99-113`**

Ampliar `roles` do item "Metas" de `["Owner"]` para `["Owner", "Gestor", "Vendedor", "Financeiro"]`.

- [ ] **Step 5: Validar build + commit**

```bash
git add src/features/goals/ src/routes/app.gestao.metas.tsx src/features/shell/config/navigation.ts
git commit -m "feat(goals): wire GoalsPage into /app/gestao/metas with role gating"
```

---

## Phase 3 — Criação e edição

### Task 3.1: Validações + engine de sugestão

**Files:**
- Create: `src/features/goals/utils/validation.ts`
- Create: `src/features/goals/engine/suggestion.ts`

- [ ] **Step 1: `validation.ts`**

Funções puras:
- `validateGoalDates({ start, end })` → `string[]` (erros).
- `validateNoDuplicate({ goals, type, scope, period, sellerId, startDate })` → `IGoal | undefined` (meta conflitante).
- `validateTarget({ value, metric })` → `string | undefined`.

- [ ] **Step 2: `engine/suggestion.ts`**

```typescript
import type { GoalMetric, ID, IGoal, IOrder } from "@/shared/types";

export interface IGoalSuggestion {
  /** Suggested target value (5% growth over previous). */
  suggestedTarget: number;
  /** Previous-period value used as base. */
  previousValue: number;
  /** Previous-period achievement (0..1+). */
  previousAchievement: number;
  /** True when no previous data exists — caller should hide the comparison. */
  hasPrevious: boolean;
}

const GROWTH_FACTOR = 1.05;

/**
 * Suggest a target value for a new goal based on the same metric achieved by
 * the same seller (or store) in the previous period. Returns `hasPrevious=false`
 * with a metric-specific default when there is no prior data.
 */
export function suggestTarget(input: {
  metric: GoalMetric;
  sellerId?: ID;
  storeId: ID;
  previousPeriodGoals: IGoal[];
  previousPeriodOrders: IOrder[];
}): IGoalSuggestion {
  // Implementation: find prior goal of same metric+scope; compute previousValue;
  // suggestedTarget = previousValue * GROWTH_FACTOR; fallback per-metric default.
  // …
  return {
    suggestedTarget: 0,
    previousValue: 0,
    previousAchievement: 0,
    hasPrevious: false,
  };
}
```

Implementar fallbacks por metric: revenue 100k, margin 25k, tickets 20, ticket_medio 800, novos_clientes 5, positivacao 30, recovery 5, conversion 50.

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/utils/validation.ts src/features/goals/engine/suggestion.ts
git commit -m "feat(goals): add form validation and smart target suggestion engine"
```

---

### Task 3.2: Form sections + `NewGoalPage`

**Files:**
- Create: `src/features/goals/components/forms/GoalConfigSection.tsx`
- Create: `src/features/goals/components/forms/GoalScopeSection.tsx`
- Create: `src/features/goals/components/forms/GoalTargetSection.tsx`
- Create: `src/features/goals/components/forms/GoalRewardSection.tsx`
- Create: `src/features/goals/pages/NewGoalPage.tsx`
- Create: `src/routes/app.gestao.metas.nova.tsx`

- [ ] **Step 1: `GoalConfigSection.tsx`**

Form controls (react-hook-form):
- Tipo (5 + 3 = 8 opções; mostrar os 5 do PRD-042 em destaque, opcionais em accordion "Avançado").
- Período (mensal/trimestral/anual).
- Datas auto-preenchidas via `useEffect` quando período muda.
- Nome autogenerado (editável).

- [ ] **Step 2: `GoalScopeSection.tsx`**

- Radio (individual/store/global). Global Owner-only.
- Se individual: dropdown vendedores (via `useSellersProvider`).
- Se store: dropdown lojas para Owner; locked para Gestor.

- [ ] **Step 3: `GoalTargetSection.tsx`**

- Input numérico — formatação BRL/count via `formatBRL`/`Number.toLocaleString`.
- Chama `suggestTarget` e mostra card: "Mês anterior: R$ X (alcançou Y%)" + botão "Aceitar sugestão".
- Comparativo visual: "X% maior/menor que a anterior" com seta + cor.

- [ ] **Step 4: `GoalRewardSection.tsx`**

- Textarea opcional. Placeholder visível ao vendedor.

- [ ] **Step 5: `NewGoalPage.tsx`**

Compõe as 4 seções. Botões:
- "Salvar rascunho" → cria com `status: "arquivada"`.
- "Criar e ativar" → cria com `status: "ativa"`.

Após salvar:
- `goalsProvider.upsert(...)`.
- `recordAuditLogSync({ actorId, action: "goal_create", resource: "goal", resourceId, storeId })`.
- `toast.success(...)` + `navigate({ to: "/app/gestao/metas/$id", params: { id } })`.

- [ ] **Step 6: Rota `src/routes/app.gestao.metas.nova.tsx`**

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { requireAuth } from "@/features/auth/guards";
import { NewGoalPage } from "@/features/goals/pages/NewGoalPage";

export const Route = createFileRoute("/app/gestao/metas/nova")({
  beforeLoad: ({ location }) => requireAuth(location.pathname, ["Owner", "Gestor"]),
  component: NewGoalPage,
});
```

- [ ] **Step 7: Validar build + commit**

```bash
git add src/features/goals/components/forms/ src/features/goals/pages/NewGoalPage.tsx src/routes/app.gestao.metas.nova.tsx
git commit -m "feat(goals): add new goal creation page with smart suggestions"
```

---

### Task 3.3: Modal de edição + cancelamento

**Files:**
- Create: `src/features/goals/components/EditGoalModal.tsx`
- Create: `src/features/goals/components/CancelGoalDialog.tsx`

- [ ] **Step 1: `EditGoalModal.tsx`**

Modal (shadcn `Dialog`) com form:
- Nome.
- Target — se diferente do original, mostra aviso prominente: "Mudança de target em meta ativa impacta comissões a partir desta data. Comissões já calculadas permanecem inalteradas." + checkbox "Entendi, confirmar".
- Recompensa.

Outros campos (tipo/escopo/sellerId/período) renderizados como read-only.

Submit:
- `goalsProvider.update(goal.id, patch)`.
- Audit `action: "goal_update"` (genérico).
- Se `targetValue` mudou: audit adicional `action: "goal_target_change"` com `before`/`after` no `metadata`.
- Toast.

- [ ] **Step 2: `CancelGoalDialog.tsx`**

`AlertDialog` com input obrigatório de motivo. Ação:
- `goalsProvider.update(id, { status: "cancelada", cancelReason })`.
- Audit `action: "goal_cancel"` com `metadata.reason`.

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/components/EditGoalModal.tsx src/features/goals/components/CancelGoalDialog.tsx
git commit -m "feat(goals): add edit and cancel flows with target-change audit"
```

---

## Phase 4 — Detalhe da meta

### Task 4.1: Header + summary + chart + history

**Files:**
- Create: `src/features/goals/components/detail/GoalDetailHeader.tsx`
- Create: `src/features/goals/components/detail/GoalProgressSummary.tsx`
- Create: `src/features/goals/components/detail/GoalEvolutionChart.tsx`
- Create: `src/features/goals/components/detail/GoalHistorySection.tsx`
- Create: `src/features/goals/utils/composition.ts`
- Create: `src/features/goals/components/detail/GoalCompositionSection.tsx`

- [ ] **Step 1: `GoalDetailHeader.tsx`**

- Nome grande + breadcrumb voltando para `/app/gestao/metas`.
- Badges: tipo, escopo (avatar do vendedor ou loja), status.
- Datas (start → end formatadas pt-BR).
- Ações: "Editar" (abre `<EditGoalModal />`), "Arquivar", "Cancelar" (abre `<CancelGoalDialog />`).

- [ ] **Step 2: `GoalProgressSummary.tsx`**

- `<GoalProgressBar size="lg" />`.
- Valor atual (font-size grande) / target.
- % atingido (font-size enorme — `text-5xl`).
- `<GoalStatusBadge mode="progress" />`.
- "Mantendo este ritmo, projeção: R$ X (Y% da meta)" via `progress.projection`.
- Dias restantes com badge de urgência (`< 5` dias → cor vermelha).

- [ ] **Step 3: `GoalEvolutionChart.tsx`**

LineChart Recharts com 2 séries: realizado (cor primária, sólida) vs esperado (cinza tracejado). Eixo X: dias do período. Buckets diários do `currentValue` acumulado para metas de revenue/tickets/margin/positivacao/novos_clientes; para `ticket_medio` mostrar média rolante. Tooltip mostra ambos valores.

Função helper `buildEvolutionSeries(goal, context)` em `utils/composition.ts` retorna `{ date: string; realized: number; expected: number }[]`.

- [ ] **Step 4: `composition.ts`**

Funções puras:
- `buildEvolutionSeries(goal, context): IEvolutionPoint[]`.
- `getContributingOrders(goal, orders): IOrder[]` — pedidos que entram no cálculo.
- `getContributingCustomers(goal, customers): ICustomer[]` — para `novos_clientes`.
- `getTicketStats(orders): { min, max, median, std }` — para `ticket_medio`.

- [ ] **Step 5: `GoalCompositionSection.tsx`**

Renderização condicional por metric:
- `revenue` / `margin` / `tickets`: tabela paginada de pedidos com link para `/app/pedidos/$id`.
- `positivacao`: lista de clientes distintos com pedidos no período, link para `/app/clientes/$id`.
- `novos_clientes`: lista de clientes criados no período.
- `ticket_medio`: card de estatísticas (min/max/mediana/desvio) + bar chart de distribuição.

- [ ] **Step 6: `GoalHistorySection.tsx`**

Lista filtrada de audit logs onde `resource === "goal"` e `resourceId === goal.id`. Usa `useAuditsProvider`. Cada item: ação (label legível), actor, timestamp, metadata (especialmente before/after de target).

- [ ] **Step 7: Validar build + commit**

```bash
git add src/features/goals/components/detail/ src/features/goals/utils/composition.ts
git commit -m "feat(goals): add detail sections with evolution chart and composition"
```

---

### Task 4.2: `GoalDetailPage` + rota

**Files:**
- Create: `src/features/goals/pages/GoalDetailPage.tsx`
- Create: `src/routes/app.gestao.metas.$id.tsx`

- [ ] **Step 1: `GoalDetailPage.tsx`**

```typescript
import { useParams, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { DashboardLayout } from "@/features/shell/layouts";
import { EmptyState } from "@/features/shell/components/EmptyState";
import { useAuth } from "@/features/auth/useAuth";
import { useGoalProgress } from "../hooks/useGoalProgress";
import { GoalDetailHeader } from "../components/detail/GoalDetailHeader";
import { GoalProgressSummary } from "../components/detail/GoalProgressSummary";
import { GoalEvolutionChart } from "../components/detail/GoalEvolutionChart";
import { GoalCompositionSection } from "../components/detail/GoalCompositionSection";
import { GoalHistorySection } from "../components/detail/GoalHistorySection";
import { EditGoalModal } from "../components/EditGoalModal";
import { CancelGoalDialog } from "../components/CancelGoalDialog";

export function GoalDetailPage() {
  const { id } = useParams({ from: "/app/gestao/metas/$id" });
  const navigate = useNavigate();
  const { userRole, currentUser } = useAuth();
  const { goal, progress, isLoading, hasError } = useGoalProgress(id);
  const [editOpen, setEditOpen] = useState(false);
  const [cancelOpen, setCancelOpen] = useState(false);

  if (isLoading) {
    return <DashboardLayout>{/* Skeleton */}</DashboardLayout>;
  }
  if (hasError || !goal || !progress) {
    return (
      <DashboardLayout>
        <EmptyState
          icon="mdi:alert-circle-outline"
          title="Meta não encontrada"
          actionLabel="Voltar"
          actionTo="/app/gestao/metas"
        />
      </DashboardLayout>
    );
  }

  // Vendedor only sees own goals
  if (userRole === "Vendedor" && goal.targetId !== currentUser?.sellerId) {
    return (
      <DashboardLayout>
        <EmptyState
          icon="mdi:shield-lock-outline"
          title="Acesso restrito"
          actionLabel="Voltar"
          actionTo="/app/gestao/metas"
        />
      </DashboardLayout>
    );
  }

  const canEdit = userRole === "Owner" || userRole === "Gestor";

  return (
    <DashboardLayout>
      <GoalDetailHeader
        goal={goal}
        progress={progress}
        canEdit={canEdit}
        onEdit={() => setEditOpen(true)}
        onCancel={() => setCancelOpen(true)}
        onBack={() => void navigate({ to: "/app/gestao/metas" })}
      />
      <div className="mt-6 flex flex-col gap-4">
        <GoalProgressSummary goal={goal} progress={progress} />
        <GoalEvolutionChart goal={goal} />
        <GoalCompositionSection goal={goal} />
        <GoalHistorySection goalId={goal.id} />
      </div>

      {canEdit && (
        <>
          <EditGoalModal
            goal={goal}
            open={editOpen}
            onOpenChange={setEditOpen}
            actorId={currentUser?.sellerId ?? "system"}
          />
          <CancelGoalDialog
            goal={goal}
            open={cancelOpen}
            onOpenChange={setCancelOpen}
            actorId={currentUser?.sellerId ?? "system"}
          />
        </>
      )}
    </DashboardLayout>
  );
}
```

- [ ] **Step 2: Rota `src/routes/app.gestao.metas.$id.tsx`**

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { requireAuth } from "@/features/auth/guards";
import { GoalDetailPage } from "@/features/goals/pages/GoalDetailPage";

export const Route = createFileRoute("/app/gestao/metas/$id")({
  beforeLoad: ({ location }) =>
    requireAuth(location.pathname, ["Owner", "Gestor", "Vendedor", "Financeiro"]),
  component: GoalDetailPage,
});
```

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/pages/GoalDetailPage.tsx src/routes/app.gestao.metas.$id.tsx
git commit -m "feat(goals): add goal detail page with 5 sections"
```

---

## Phase 5 — Status automático, notificações, widget e release

### Task 5.1: Status automático + notificações de marco

**Files:**
- Create: `src/features/goals/hooks/useGoalAutoStatusUpdate.ts`
- Create: `src/features/goals/hooks/useGoalMilestoneToast.ts`

- [ ] **Step 1: `useGoalAutoStatusUpdate.ts`**

Hook que roda no mount do `GoalsPage` (Gestor/Owner apenas para evitar repetição):
- Busca todas as metas com `status: "ativa"` e `endDate < now`.
- Para cada uma: calcular progress; se `percentage >= 100` → `goalsProvider.update(id, { status: "concluida" })` + audit `goal_auto_complete`; senão → `status: "arquivada"` + audit `goal_auto_archive`.
- Roda uma vez por sessão (guard via `useRef`).

- [ ] **Step 2: `useGoalMilestoneToast.ts`**

Hook para o vendedor:
- Mantém `localStorage` key `gallo-goal-milestones` mapeando `goalId → number[]` (marcos já notificados).
- Para cada meta com `useGoalsWithProgress`:
  - Se `percentage >= 100` e 100 não foi notificado → toast 🏆.
  - Se `>= 80` e 80 não foi notificado → toast 🚀.
  - Se `>= 50` e 50 não foi notificado → toast 🎯.
  - Atualiza localStorage.
- Usa labels do i18n.

Invocado no `<IndividualGoalsDashboard />`.

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/hooks/useGoalAutoStatusUpdate.ts src/features/goals/hooks/useGoalMilestoneToast.ts
git commit -m "feat(goals): add auto status transitions and milestone toasts"
```

---

### Task 5.2: Widget no PRD-014 (Painel Gestor)

**Files:**
- Create: `src/features/goals/components/widget/GoalsWidget.tsx`
- Modify: `src/features/manager-dashboard/pages/ManagerDashboardPage.tsx`

- [ ] **Step 1: `GoalsWidget.tsx`**

Card compacto. Recebe `storeId`. Usa `useStoreGoals(storeId)`. Renderiza:
- Header: "Metas do mês" + link "Ver todas" para `/app/gestao/metas`.
- Lista compacta (top 5 ordenadas por menor % primeiro — mais urgentes).
- Cada item: nome (truncate), barra mini (`size: "sm"`), valor compacto, %.
- Click → `/app/gestao/metas/$id`.

- [ ] **Step 2: Integrar em `ManagerDashboardPage.tsx`**

Importar `GoalsWidget` de `@/features/goals` e adicionar no grid abaixo de `VolumeHeatmap` (ou seção análoga). Manter responsividade.

- [ ] **Step 3: Validar build + commit**

```bash
git add src/features/goals/components/widget/ src/features/manager-dashboard/pages/ManagerDashboardPage.tsx
git commit -m "feat(goals): add 'Metas do mês' widget to manager dashboard"
```

---

### Task 5.3: Version bump v0.26.0 Pulse + CHANGELOG + INDEX + rename PRD

**Files:**
- Modify: `package.json`
- Modify: `CHANGELOG.md`
- Modify: `docs/prds/INDEX-PRDs-Gallo-Base-Diesel.md`
- Rename: `docs/prds/PRD-042-metas.md` → `docs/prds/PRD-042-metas_DONE.md`

- [ ] **Step 1: Bump `package.json` para `"version": "0.26.0"`.**

- [ ] **Step 2: Adicionar entrada `## [0.26.0] — Pulse · 2026-05-26` no `CHANGELOG.md`.**

Estrutura: parágrafo de contexto (motivação) + 1-2 parágrafos descrevendo entregas + seções `### Added` (componentes/hooks/engine) e `### Changed` (rota, navigation, tipo IGoal). Seguir o padrão das entradas v0.24.0 e v0.25.0.

- [ ] **Step 3: Atualizar `INDEX-PRDs-Gallo-Base-Diesel.md`**

- Linha PRD-042 → marcar ✅ + adicionar link `_DONE.md`.
- Contadores "Implementação": 25/50 (50%) / 25 pendentes.
- Adicionar entrada v0.26.0 Pulse no histórico de versões.
- "Última Atualização": data, motivo, versão índice 1.5.

- [ ] **Step 4: Rename do PRD**

Run: `cd D:/claude/gallo-basediesel && git mv docs/prds/PRD-042-metas.md docs/prds/PRD-042-metas_DONE.md`

- [ ] **Step 5: Build + lint final**

Run: `cd D:/claude/gallo-basediesel && bun run build 2>&1 | tail -5 && echo "---" && bun run lint 2>&1 | grep -i "goals" | head -20`
Expected: build verde; zero erros novos em `src/features/goals/`.

- [ ] **Step 6: Commit final**

```bash
git add package.json CHANGELOG.md docs/prds/
git commit -m "chore(release): v0.26.0 Pulse — PRD-042 Metas"
```

---

## Verification

**Manual (sem dev-browser, conforme solicitado pelo usuário):**

1. `bun run build` — zero erros TypeScript; chunk `app.gestao.metas-*.js` gerado.
2. `bun run lint` — zero erros novos no escopo `src/features/goals/`.
3. (Opcional) `bun run dev` em background — usuário valida visualmente.

**Checklist Gherkin do PRD (validação visual a critério do usuário):**

- Como Vendedor: `/app/gestao/metas` mostra cards individuais; sem dashboard agregado.
- Como Gestor/Owner: KPIs no topo, tabela, bar chart por vendedor; botão "Nova meta" leva para `/nova`.
- Em `/app/gestao/metas/nova`: ao escolher tipo+vendedor, sugestão "Mês anterior: R$ X (alcançou Y%)" aparece e botão "Aceitar sugestão" preenche o input.
- Em `/app/gestao/metas/$id`: 5 seções (header, summary, chart, composition, history).
- Mudança de target em meta ativa exibe aviso sobre comissões + checkbox confirmação.
- Cancelar meta exige motivo no `AlertDialog`.
- Hook `useGoalAutoStatusUpdate` transiciona metas vencidas em uma única sessão.
- Vendedor com meta ≥ 50/80/100% recebe toast (1× por marco por meta — controle em localStorage).
- Widget "Metas do mês" aparece no Painel Gestor (`/app/inicio`).
- PRD-014 não regrediu — KPIs e gráficos originais continuam funcionando.

---

## Self-Review

**1. Spec coverage:**
- ✅ RF-001 a RF-003: tipo + mocks (Task 1.1, 1.2).
- ✅ RF-004 a RF-008: engine + projection (Task 1.3).
- ✅ RF-009 a RF-010: hooks (Task 1.4) + extras `useGoalsWithProgress`, `useGoalsStatistics`.
- ✅ RF-011 a RF-014: GoalsPage com dashboards individual e agregado (Tasks 2.1–2.5).
- ✅ RF-015 a RF-020: NewGoalPage (Tasks 3.1–3.2).
- ✅ RF-021 a RF-022: GoalDetailPage (Tasks 4.1–4.2).
- ✅ RF-023 a RF-025: EditGoalModal (Task 3.3).
- ✅ RF-026 a RF-028: useGoalAutoStatusUpdate + useGoalMilestoneToast (Task 5.1).
- ✅ RF-029: widget em ManagerDashboardPage (Task 5.2).
- ✅ RF-030 a RF-031: hooks exportados em `index.ts` (Task 2.5).
- ✅ RF-032 a RF-034: permissões resolvidas em GoalsPage + rotas (Tasks 2.5, 3.2, 4.2).
- ✅ RF-035: audit em todas as mutações (Tasks 3.2, 3.3, 5.1).

**2. Placeholder scan:** sem TBD/TODO; cada step tem código ou caminho exato.

**3. Type consistency:** `GoalMetric` ampliado conserva backward-compat; `IGoalProgress` consistente em todos os hooks; `useGoalsWithProgress` e `useGoalProgress` retornam o mesmo tipo `IGoalProgress`. `calculateGoalProgress` assinatura única.

**4. Notas sobre TDD:** o projeto não tem suite de testes configurada (`CLAUDE.md` confirma). Cada task valida via `bun run build` (TypeScript noEmit) + `bun run lint`. Validação funcional é responsabilidade do usuário ao final.

---
