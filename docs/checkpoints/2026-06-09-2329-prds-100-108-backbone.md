# Checkpoint — Closeout PRDs 100–108 + v0.74.0 Backbone + #46 validado — 2026-06-09T23:29-03:00

> **Branch:** `main` · **Último commit:** `cb0056b` docs(fase2): mark A2 (#46) as done
> **Sessão anterior:** Claude (Opus) · **Gerado em:** 2026-06-09T23:29-03:00

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-06-09-2329-prds-100-108-backbone.md` na íntegra e confirme em uma frase que entendeu:
1) o objetivo da sessão, 2) o estado atual do código, 3) qual é a próxima tarefa.
Não faça nenhuma ação até eu autorizar.
```

---

## 📌 Contexto do projeto

GALLO BASE DIESEL — SaaS de inteligência comercial (Vite + React 19 + TS strict, TanStack Router/Query, Tailwind v4/shadcn, Supabase via MCP, projeto ref `njizaasajkdqptlxddqn`). Fase 2 (Mock → Supabase) com escopo A mergeado desde a v0.73.0; esta sessão fechou a **faixa de infra PRD-100–108**.

## 🎯 Objetivo da sessão

1. Auditar todos os PRDs sem sufixo `_DONE` e validar fase a fase contra o estado real (pedido inicial).
2. "Resolver os PRDs até o 108": planejar e executar o que faltava de 100–108, com cuidado com regressões.
3. Ativar o convite por e-mail (#46): secrets Resend + allowlist + teste e2e.

## ✅ Progresso (o que foi feito)

- [x] **Auditoria dos 29 PRDs não-DONE** (5 agentes + verificação MCP no banco) → relatório entregue; PRD-001/103/104 renomeados `_DONE`, commit `f8bb065`.
- [x] CLAUDE.md sincronizado com a Fase 2 real, commit `463b7c5`.
- [x] **Etapa 1 (PRD-100/101)** `1a0b292` — 55 migrations exportadas byte a byte do histórico remoto p/ `supabase/migrations/` + README com a regra de export; `src/types/supabase.generated.ts`; `docs/db/schema-overview.md`; runbooks `docs/infra/`; workflows `db-deploy.yml`/`gen-types.yml` (no-op até secrets).
- [x] **Etapa 2 (PRD-102)** `7f6ea18` — `supabase/functions/_shared/` (cors/http/env/logger/serve/auth/audit); canário `hello-trace`; 5 functions refatoradas **e redeployadas (v3)** com smoke verde (401 invalid session + x-trace-id = boot OK); `edge-deploy.yml`; `docs/edge/conventions.md`.
- [x] **Etapa 3 (PRD-107)** `ba835a6` — rota `/auth/definir-senha` (SetPasswordPage) + `inviteSellerByEmail` + botão "Convidar por e-mail" no CreateAccessDialog.
- [x] **Etapa 4 (PRD-105 core)** `6659875` — migration `realtime_105_conversations_messages` (publication); `src/shared/lib/realtime.ts` (manager ref-counted); `useRealtimeConversations` com gate por `getActiveDataSource()` (mock = simulador intacto); indicador âmbar "conectando" no RealtimeToggle.
- [x] **Etapa 5 (PRD-106)** `650874d` — migration `storage_106_buckets_policies` (6 buckets + 12 policies); upload real de bytes (galeria → `whatsapp-media`), signed URL real (5 min), delete remove objeto; `IMediaUploadInput.file?`; botão "Enviar mídia" nas galerias.
- [x] **Etapa 6 (PRD-108)** `e2ba6ad` — migration `perf_108_trgm_matviews` (pg_trgm + 5 GIN + parcial leads; 3 MVs BI + refresh pg_cron 15min + RPCs `mv_*_read()` scoped); `docs/db/performance-baseline.md` + `performance.md`; seção #108 na suíte RLS.
- [x] PRDs 100–108 renomeados `_DONE` com notas de ressalva, `0aa80fd`.
- [x] **PR #50 MERGED** na main (`48ebbbe`); **release `v0.74.0 Backbone`** (`638f5a4`, tag pushada): CHANGELOG (UI), package.json, CLAUDE.md.
- [x] **#46 FECHADO** — secrets Resend setados pelo dono, allowlist configurada, **teste e2e passou** (convite → e-mail → definir senha → login); doc mestre atualizado `cb0056b`; issue fechado no GitHub.

## 🔧 Estado do código

- **Branch:** `main` @ `cb0056b` (tudo pushado exceto este checkpoint; tag `v0.74.0` pushada)
- **Migrations no remoto:** 58 (todas espelhadas em `supabase/migrations/`)
- **Build/testes:** `bun run build` OK · 250/250 Vitest · suíte RLS por impersonação PASSED (incl. MVs) · advisors sem alertas novos além dos WARNs intencionais documentados
- **Jobs pg_cron ativos:** `reconcile-derived-notifications` (1 min) · `refresh-bi-matviews` (15 min)
- **Edge Functions (todas v3+, ACTIVE):** hello-trace, invite-seller, invite-seller-email (ATIVA com Resend), reset-seller-password, set-seller-access, set-seller-role

## ⏳ Pendências (próximos passos, em ordem)

1. **#45 — secrets de CI** (dono): `SUPABASE_DB_URL`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, `SUPABASE_DB_PASSWORD` no GitHub → ativa de uma vez `rls-tests`/`db-deploy`/`gen-types`/`edge-deploy`. Pronto = PR tocando `supabase/tests/**` roda a suíte de verdade.
2. **Rotacionar a `RESEND_API_KEY`** (vazou em print no chat durante a config) — runbook `docs/infra/rotate-keys.md`. 5 min.
3. **#47 — flip de produção** (decisão do dono): Vercel Production → `VITE_DATA_SOURCE/AUTH_SOURCE=supabase` + smoke (`docs/db/cutover-smoke-checklist.md`). Ao flipar: conferir `INVITE_REDIRECT_URL` apontando para `https://gallobasediesel.com.br/auth/definir-senha` (pode ter ficado em localhost após o teste e2e).
4. **PRD-109 (backup/DR) e PRD-110 (monitoring)** — únicos não-DONE até 110; gap real de go-live (sem backup testado nem observabilidade). Candidatos a próxima sessão de implementação.
5. Backlog futuro: PRD-111–120 (WhatsApp real), 124–129 (DINTEC/NF-e), 201, loja transacional (#40/#41).

## ❓ Decisões pendentes

- **Quando flipar produção (#47)** — pré-requisitos técnicos satisfeitos desde a Keystone; agora com convite por e-mail funcional. Decisão de negócio do dono.
- **Wiring das MVs nos dashboards** — RPCs prontas (`mv_*_read()`); dashboards seguem computando dos providers. Trocar a fonte quando o volume justificar (documentado em `docs/db/performance.md`).

## 🚧 Bloqueios / Riscos

- **Drift consciente (#44):** condições de notificação derivada vivem em TS (`derivedConditions.ts`) E na função SQL — mudanças de regra tocam os dois.
- **MVs sem RLS:** padrão obrigatório para MVs novas = unique index + revoke + RPC scoped + entrada no job de refresh (ver `docs/db/performance.md`).
- **Regra de migrations:** todo `apply_migration` via MCP deve ser exportado para `supabase/migrations/` no mesmo PR (README do diretório).
- **Dev server zumbi** regenera `routeTree.gen.ts` mais rápido que `git pull` — se o pull travar: `git reset --hard origin/main` (seguro quando main local não tem commit exclusivo).

## ⚠️ Avisos do usuário (regras desta sessão)

- service_role NUNCA no client; **não ler o valor** (vive em `.env.local` sem `VITE_`, gitignored).
- Extensões/infra compartilhada só com consentimento explícito — `pg_cron` e `pg_trgm` autorizados; **pgTAP segue NÃO autorizado**.
- Escrita anônima (anon write policies) é PROIBIDA.
- Usuário testa UI manualmente — não abrir browser/preview para validar.
- `src/routeTree.gen.ts` é ruído gerado (descartar antes de merges).
- Untracked alheios (INDEX v1_4, PRDs 111–129, `relatorio-codigo-morto`, `knip.json`) — NÃO commitar.
- Escopos decididos pelo dono nesta sessão: Realtime = core (conversas+mensagens); Storage = infra + UI de upload; 108 sem cursor/cache/k6.
- CRLF warnings do git são falso-positivo conhecido.

## 🛡️ Não regredir (features que devem continuar funcionando)

- Modo `mock` (default de produção): simulador de realtime do Inbox, wizard de checkout demo, mock de mídia — todos intactos (gates por `getActiveDataSource()`/`AUTH_SOURCE`).
- Modo `supabase`: login real + claims; isolamento per-seller (Slices 1–4, #43, #48); pool de conversas; storefront anon read-only; handoff WhatsApp no checkout; tela Usuários completa (criar/convidar/reset/desligar/papel); reconciler server-side.
- As 6 Edge Functions ACTIVE; contratos de erro preservados (strings são contrato com o client).

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/fase2-pendencias.md` — doc mestre (A1/#45 e A3/#47 abertos; resto fechado)
- `docs/db/schema-overview.md` · `docs/db/performance.md` · `docs/edge/conventions.md` — estado da infra
- `supabase/migrations/README.md` — regra de export de migrations
- `docs/prds/PRD-109-backup-dr.md` + `PRD-110-monitoring.md` — próximos PRDs se a infra continuar
- `CLAUDE.md` — estado do projeto atualizado (v0.74.0 Backbone)

## 🧠 Memórias relacionadas

- `project_fase2_supabase_kickoff` (atualizada com o closeout + release), `project_routetree_merge_block`, `project_devserver_stale_branch`, `project_tsc_baseline_errors`, `feedback_manual_testing`.

## 📚 Referências

- PR #50 (MERGED): https://github.com/edmilson-prog/gallo-basediesel/pull/50
- Issues: #45 (aberto), #46 (fechado nesta sessão), #47 (aberto)
- Tags: `v0.73.0` Keystone · `v0.74.0` Backbone
