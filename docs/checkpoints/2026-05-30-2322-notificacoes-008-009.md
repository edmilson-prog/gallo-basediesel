# Checkpoint — Épico de Notificações (PRD-008 Herald + PRD-009 Chime) — 2026-05-30T23:22

> **Branch:** `claude/trusting-hertz-bfa7df` · **Último commit (antes do checkpoint):** `f416462` feat(notifications): factory, context, hooks, ESLint isolation and provider mount
> **Sessão anterior:** Claude (brainstorming → design visual → planos → execução parcial) · **Gerado em:** 2026-05-30T23:22 (-03)

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-05-30-2322-notificacoes-008-009.md` na íntegra e confirme em uma frase que entendeu:
1) o objetivo da sessão, 2) o estado atual do código, 3) qual é a próxima tarefa.
Não faça nenhuma ação até eu autorizar.
```

---

## 📌 Contexto do projeto

GALLO BASE DIESEL — plataforma SaaS de inteligência comercial (React 19 + TanStack Router file-based + Vite + Tailwind v4 + shadcn/ui + Iconify; dados via Provider Pattern com mocks, `VITE_DATA_SOURCE=mock`). Fase 1 frontend-first. Estamos construindo o **épico "Sistema de Notificações"**: PRD-008 (fundação invisível) → PRD-009 (UI), nessa ordem obrigatória.

## 🎯 Objetivo da sessão

Planejar e implementar o épico de notificações. Concluído nesta sessão: brainstorming + resolução de decisões + design visual (via companion + ui-ux-pro-max + magic) + **2 planos de implementação** + **execução das Fases 1-2 do PRD-008**. A sessão foi pausada (escolha do usuário) num marco real para retomada em contexto limpo, dado o volume restante (~28 tarefas).

## ✅ Progresso (o que foi feito)

**Planejamento (commitado):**
- [x] Spec de design consolidado — commit `0b32815` — `docs/superpowers/specs/2026-05-30-notificacoes-008-009-design.md`
- [x] Plano 008 + Plano 009 — commit `7cc3c47` — `docs/superpowers/plans/2026-05-30-notificacoes-008-fundacao.md` e `-009-ui.md`
- [x] PRDs-fonte rastreados no git (estavam untracked!) — commit `<este checkpoint precede>` (docs: track PRD-008/009 source documents)

**PRD-008 — Fase 1 (Modelo) — COMPLETA:**
- [x] Task 1.1 — `INotification` + unions + barrel + events stub — commit `dfeb261`
- [x] Task 1.2 — glossário + delta PRD-002 — commit `d4a057a`

**PRD-008 — Fase 2 (Persistência) — COMPLETA:**
- [x] Task 2.1 — contratos (`INotificationStore`, `INotificationPreferenceStore`, `_shared`, `errors`) — commit `a848ce7`
- [x] Task 2.2 — generator + mock api + bootstrap (~264 notificações; seed isolado por offset primo 7919, preserva seeds existentes) — commit `4d64492`
- [x] Task 3.4 (antecipada — dependência de 2.3) — defaults Anexo B por papel + `isChannelLocked`/`isCategoryFullyOptional` — commit `aeb77e9`
- [x] Task 2.3 — impls mock/supabase + RBAC scope não-burlável (own/store/all) + audit via barrel data + ESLint exceção impl/mock — commit `09152f5`
- [x] Task 2.4 — factory (singleton), context (+ boot do reconciler placeholder), hooks TanStack Query, barrel isolado, ESLint restrição, mount em `__root.tsx` — commit `f416462`

**Resultado:** a camada de DADOS de notificações está viva. Os hooks `useNotifications` / `useUnreadCount` / `useNotificationPreferences` retornam dados reais dos seeds. `bun run build` verde após cada task.

## 🔧 Estado do código

- **Branch:** `claude/trusting-hertz-bfa7df` (worktree). Ahead da `main` por ~12 commits (9 desta linha + checkpoint).
- **Build:** `bun run build` PASS (4907 módulos, último check na Task 2.4).
- **Estrutura criada:** `src/providers/notifications/` com `factory.ts`, `context.tsx`, `errors.ts`, `events.ts` (STUB — só `"conversa.atribuida"`), `reconciler.ts` (PLACEHOLDER no-op), `contracts/`, `impl/{mock,supabase}/`, `hooks/`, `preferences/defaults.ts`, `index.ts` (barrel público). Mocks: `src/mocks/generators/notification.ts`, `src/mocks/api/notifications.ts` + wiring em `bootstrap.ts`/`store/*`/`api/index.ts`.
- **PRs abertos:** nenhum até o checkpoint (será criado um draft).

## ⏳ Pendências (próximos passos, em ordem) — retomar via `superpowers:subagent-driven-development`

> Cada task tem o texto completo + código no Plano 008 (`docs/superpowers/plans/2026-05-30-notificacoes-008-fundacao.md`). Despache um subagente por task; valide por `bun run build` + eslint per-file; comite; siga.

**PRD-008 restante:**
1. **Task 3.1** — `bus.ts` (`notificationBus` emit/subscribe, não-bloqueante). Feito = build verde + commit.
2. **Task 3.2** — expandir `events.ts` para a união completa do **Anexo A** (30 eventos) + `DERIVED_EVENTS`.
3. **Task 3.3** — `routing/dedupe.ts` + `routing/rules.ts` (mapa evento→categoria/severidade/recipients/canais).
4. **Task 3.5** — `routing/router.ts` + ligar `startRouter(value)` no boot `useEffect` de `context.tsx`.
5. **Task 4.1** — `channels/contract.ts` + `channels/registry.ts` (`ACTIVE_CHANNELS=["inApp","toast"]`).
6. **Task 4.2** — `channels/inApp.ts` + `channels/toast.ts` (ativos; toast via sonner).
7. **Task 4.3** — `channels/{email,whatsapp,sms,push}.ts` (stubs `deferred`, `send` direto lança erro → PRDs 141/143/144/145).
8. **Task 4.4** — religar toasts existentes via ToastChannel (sem mudar UX visível).
9. **Task 5.1** — extrair `IActiveAlert`+`build*` de `useActiveAlerts.ts` para `conditions/derivedConditions.ts`; `useActiveAlerts` passa a importar do barrel `@/providers/notifications`. ⚠️ manager dashboard deve continuar idêntico.
10. **Task 5.2** — `reconciler.ts` real (substitui o placeholder; lê `settings.alertPollingSeconds` + thresholds de `IManagerDashboardSettings`; mapeia `IActiveAlert`→`INotification` derived; `reconcileDerived`).
11. **Task 5.3** — `groupKey` no router + seeds.
12. **Task 5.4** — harness dev-only em `src/routes/design-system.tsx` (emitir eventos, logar roteamento no console).
13. **Task 5.5** — nota de migração no PRD-014.
14. **Release Herald** — bump MINOR, `CHANGELOG.md`, índice PRDs, renomear `PRD-008-...md`→`_DONE.md`, `bun run build` final.

**Depois: Plano 009 inteiro** (`docs/superpowers/plans/2026-05-30-notificacoes-009-ui.md`) — 5 fases (tokens severidade → sino/dropdown → página+alternador → preferências → migração ActiveAlertsList/toasts → portal cliente).

## ❓ Decisões pendentes

Todas as decisões de design/arquitetura foram **resolvidas** nesta sessão (ver spec). Observações deixadas pelos subagentes para revisar durante a implementação:
- `reconcileDerived` no mock api (Task 2.2) tem redundância menor (inócua) — ajustar quando o reconciler real (Task 5.2) for implementado.
- Sessão de cliente "viva" não existe na Fase 1 (`recipientType:'customer'` só via seed) — caminho de escopo está correto mas não exercitado em runtime até o portal (PRD-009 Fase 5).

## 🚧 Bloqueios / Riscos

- **CRLF lint (ambiental):** `bun run lint` global reporta ~143k erros `prettier/prettier` de CRLF **pré-existentes** em todo o repo (Windows + git autocrlf). NÃO é do nosso código. **Validar por `bun run build` (tsc, canônico) + `npx eslint <arquivos tocados>` para erros reais.** Não rodar `bun run lint` global como porta de validação.
- **Upstream da branch aponta para `origin/main`:** NUNCA fazer `git push` cego (empurraria/recusaria contra main). Push explícito: `git push -u origin claude/trusting-hertz-bfa7df`.

## ⚠️ Avisos do usuário (regras desta sessão)

- **Testar UI manualmente** — NÃO abrir browser/devtools/preview para "validar" mudanças (memória `feedback_manual_testing.md`). O companion visual de brainstorming foi exceção pedida pelo usuário; encerrado.
- **Supply-chain guard:** `bunfig.toml` impõe `minimumReleaseAge=86400` (24h). Confirmar com o usuário antes de adicionar pacote a `minimumReleaseAgeExcludes`. (O épico não precisa de libs novas.)
- **Idioma:** código/comentários em inglês; UI/conteúdo/docs em pt-BR com acentos corretos (UTF-8).
- **Git:** não commitar/pushar em `main` sem confirmação. Co-author dos commits: `Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **Não editar `src/routeTree.gen.ts`** manualmente (gerado pelo plugin).

## 🛡️ Não regredir (deve continuar funcionando)

- **Painel do Gestor (PRD-014):** `useActiveAlerts` + `<ActiveAlertsList>` devem continuar exibindo os 3 alertas idênticos. A Task 5.1 só EXTRAI a lógica de condição (muda imports, não comportamento). A migração visual para o center é PRD-009 RF-029 (Plano 009).
- **Seeds de mocks existentes:** preservados bit-a-bit (offset primo na Task 2.2). Não quebrar.
- **Árvore de providers no `__root.tsx`:** `Query > Theme > Data > Notification > Auth > Multistore > Outlet`. Não reordenar sem cuidado.
- **Build verde:** manter `bun run build` limpo a cada task.

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/superpowers/plans/2026-05-30-notificacoes-008-fundacao.md` — o plano a executar (a partir da Task 3.1).
- `docs/superpowers/plans/2026-05-30-notificacoes-009-ui.md` — o plano seguinte (UI).
- `docs/superpowers/specs/2026-05-30-notificacoes-008-009-design.md` — decisões + design visual aprovado.
- `docs/prds/PRD-008-fundacao-notificacoes.md` e `PRD-009-notification-center-preferencias.md` — requisitos + Anexos A/B.
- `src/providers/notifications/` — o que já foi construído (espelha `src/providers/data/`).
- `src/providers/data/` — o PADRÃO a continuar espelhando (factory/context/contracts/hooks/impl).
- `src/features/manager-dashboard/hooks/useActiveAlerts.ts` — fonte da lógica a extrair (Task 5.1).
- `src/shared/types/platform.ts` — `IManagerDashboardSettings` (limiares/cadência das derivadas).
- `src/features/shell/components/TopBar.tsx` — sino placeholder a substituir (Plano 009).
- `eslint.config.js` — regras de isolamento dos providers.
- `CLAUDE.md` — convenções do projeto.

## 🧠 Memórias relacionadas

- `feedback_manual_testing.md` — usuário testa UI manualmente; não abrir preview para validar.

## 📚 Referências

- Spec: `docs/superpowers/specs/2026-05-30-notificacoes-008-009-design.md`
- Mockups visuais (gitignored, podem não persistir): `.superpowers/brainstorm/2780-1780186128/content/*.html` — as decisões estão capturadas no spec (Direção B, alternador Painel/Lista, matriz Abordagem A, portal tema PARTS).
- Codinomes: PRD-008 = **Herald**; PRD-009 = **Chime**.
