# Checkpoint — PRD-034 Catálogo de Modelos (PR #25) — 2026-06-03T14:16

> **Branch:** `feat/prd-034-catalogo-modelos` · **Último commit:** `42b7cca` chore: bump version to v0.63.0 Catalog and update changelog
> **Sessão anterior:** Claude Opus 4.8 (1M context) · **Gerado em:** 2026-06-03-1416

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-06-03-1416-prd034-modelos-pr25.md` na íntegra e
confirme em uma frase que entendeu: 1) o objetivo da sessão, 2) o estado atual do
código, 3) qual é a próxima tarefa. Não faça nenhuma ação até eu autorizar.
```

---

## 📌 Contexto do projeto

GALLO BASE DIESEL — SaaS de inteligência comercial para distribuidora de peças
pesadas (Volvo/Scania/Mercedes-Benz/Ford Cargo/Iveco). Fase 1 (Frontend First):
SPA React 19 + TS strict, Vite, TanStack Router (file-based) + Query, Tailwind v4 +
shadcn, Iconify, zod + react-hook-form, sonner, Bun. Dados mock in-memory via
Provider Pattern (drop-in Supabase na Fase 2). Esta sessão trabalhou o **épico
"Composição por Modelo (Kits)"**, composto por 3 sub-projetos sequenciais:
**PRD-034 (catálogo canônico de modelos)** → **PRD-035 (kits por modelo)** →
**delta PRD-016 (`IVehicle.modelId` + aplicar kit no veículo)**.

## 🎯 Objetivo da sessão

Implementar, em sequência, PRD-034 + PRD-035 + o delta. Nesta sessão entregamos
**apenas o sub-projeto 1 (PRD-034)** — o catálogo canônico `IVehicleModel`
gerenciável em `/app/kits`, que é a base sobre a qual os kits (PRD-035) e a frota
(delta 016) vão se apoiar. **Decisão estratégica do usuário: CONSOLIDAR** — haverá
um único conceito de kit; o `IServiceKit` ("Kits de revisão", v0.62.0) será evoluído
para o kit-pendurado-no-modelo no PRD-035. O PRD-034 **não tocou** no `IServiceKit`.

## ✅ Progresso (o que foi feito)

Fluxo completo brainstorm → spec → plano → execução subagent-driven → revisão
holística → versão → PR. 15 commits na branch (todos pushados):

- [x] Investigação dos 2 deltas divergentes (sem commit — análise): `DELTAS-...md`
      (v1.2 = Copiloto/PRD-025) vs `DELTAS-...(1).md` (v1.2 = Kits/PRD-034-035).
      **Nenhum é superconjunto do outro** → pendência de mesclar numa v1.3 ao fim do épico.
- [x] Consulta ao agente de design (layout, avatares de marca, a11y) — sem commit.
- [x] Spec aprovado, commit `1c18e3c` — `docs/superpowers/specs/2026-06-03-catalogo-modelos-prd034-design.md`
- [x] Plano (12 tarefas), commit `fbbe5f1` — `docs/superpowers/plans/2026-06-03-catalogo-modelos-prd034.md`
- [x] Task 1 tipo `IVehicleModel`, `2aeb1ed`
- [x] Task 2 helper `getBrandIcon`, `d348e93`
- [x] Task 3 seed canônico (21 modelos, 5 marcas, por expansão de motores), `1268f59`
- [x] Task 4 mock API `vehicleModelsApi` (CRUD + dedup), `49dbcfa`
- [x] Task 5 provider (contract/mock/stub/hook/factory), `3dbad9d`
- [x] Task 6 RBAC resource `vehicleModel`, `4a7196f`
- [x] Task 7 hooks query/mutação + audit, `474ded3`
- [x] Task 8 form + zod, `0d03b3b` (+ fix `04fcce4` engine register/focus)
- [x] Task 9 componentes de lista (avatar/chips/linha/grupo/delete), `e47d8c7`
- [x] Task 10 páginas (lista/form/detalhe + barrel), `6b951a9`
- [x] Task 11 rotas `app.kits.*` + nav "Kits por modelo" + cleanup de casts, `2448a65`
- [x] Revisão holística final (opus): **READY_TO_MERGE**, sem achados (sem commit).
- [x] Task 12 versão **v0.63.0 "Catalog"** + PRD renomeado `_DONE`, `42b7cca` + tag `v0.63.0` (local)
- [x] Branch pushada; **PR #25 aberto** contra `main`.

Cada task implementada por subagente fresco (sonnet) + revisão spec→qualidade
(sonnet); correções via re-dispatch. Revisão holística final em opus.

## 🔧 Estado do código

- **Branch:** `feat/prd-034-catalogo-modelos` — 15 commits ahead da `main`; sincronizada com `origin` (HEAD == upstream == `42b7cca`).
- **Tag:** `v0.63.0` criada **localmente** em `42b7cca` (NÃO empurrada ainda).
- **Build:** PASS (`bun run build` ✓ ~16s). `bunx tsc --noEmit` filtrado pela feature = vazio (o repo tem ~316 erros tsc pré-existentes não relacionados; `bun run build` NÃO type-checa).
- **Arquivos principais criados (feature `src/features/vehicle-models/`):** `utils/brandIcon.ts`, `utils/modelValidation.ts`, `hooks/{useVehicleModels,useVehicleModel,useVehicleModelMutations}.ts`, `components/{BrandAvatar,BrandFilterChips,VehicleModelRow,BrandGroup,VehicleModelForm,DeleteVehicleModelDialog}.tsx`, `pages/{VehicleModelsListPage,VehicleModelFormPage,VehicleModelDetailPage}.tsx`, `index.ts`.
- **Plataforma:** `src/shared/types/vehicle-models.ts` (+barrel); `src/mocks/data/seedVehicleModelsCanonical.ts`; `src/mocks/api/vehicleModels.ts` (+barrel); `src/providers/data/{contracts,impl/mock,impl/supabase}/vehicleModels.ts` + hook + factory + barrels; RBAC `resources.ts`/`matrix.ts`/`RolesPage.tsx`; rotas `src/routes/app.kits.{tsx,index,novo,$modelId,$modelId.editar}`; `shell/config/{routes,navigation}.ts`; `routeTree.gen.ts` regenerado.
- **PRs abertos:** **#25** — "feat: catálogo de modelos de veículo (PRD-034) — v0.63.0 Catalog" — https://github.com/edmilson-prog/gallo-basediesel/pull/25
- **Untracked (NÃO commitados — são inputs do PRD-035, pré-existentes):** `docs/prds/PRD-035-kits-composicao.md`, `docs/prds/DELTAS-PRDs-Gallo-Base-Diesel (1).md`, `docs/prds/delta-escopo-erp-gallo.md`, `docs/export/`, `docs/reports/*.pdf`.

## ⏳ Pendências (próximos passos, em ordem)

1. **Usuário vai mergear o PR #25** (decisão dele; aguardando). Critério de feito: PR fechado/mergeado na `main`.
2. **Após o merge — finalizar release (eu faço):** `git checkout main && git pull`; confirmar PRD-034 na `main`; **finalizar a tag `v0.63.0`**: se o merge preservou `42b7cca` (merge-commit/rebase), `git push origin v0.63.0`; se foi **squash**, re-apontar a tag para o commit final da `main` e empurrar. Opcional: apagar a branch local `feat/prd-034-catalogo-modelos`.
3. **Iniciar PRD-035 (kits por modelo) a partir da `main` atualizada** — brainstorm → spec → plano → subagent-driven. Inputs: `docs/prds/PRD-035-kits-composicao.md` (untracked) e `DELTAS-...(1).md`. Escopo do PRD-035 inclui:
   - Entidade `IVehicleModelKit` (`modelId`, `status` rascunho/oficial, items `{partId, defaultQuantity, isOptional, note}`) + `appliedKitIds?` em `IQuote`.
   - **CONSOLIDAÇÃO**: evoluir o `IServiceKit` existente → kit-pendurado-no-modelo; **migrar** a tela `/app/catalogo/kits` para dentro do detalhe `/app/kits/$modelId` (a seção "Kits deste modelo" hoje é slot vazio); **redirect** `/app/catalogo/kits → /app/kits`; remover o item de menu "Kits de revisão" antigo; atualizar `KitPicker`/`expandKitToItems` do editor de orçamento; renomear resource RBAC `serviceKit → modelKit`.
   - Detecção de drift; curadoria (promover/despromover); aplicação no orçamento (modal de preview + snapshot + `appliedKitIds`); sugestão automática por `modelId`; SDR placeholder.
   - **Placeholder de IA (RF-016):** botão **desabilitado** "Sugerir composição (IA)" + tooltip "Disponível na Fase 2" no editor de kit. (DECIDIDO: só no PRD-035, não no PRD-034.)
   - Trazer o agente de design para o editor de kit / modal de preview.
4. **Delta PRD-016** (depois do 035): `IVehicle.modelId` + card "Filtros" do detalhe do veículo aplica o kit; "Peças compatíveis" reflete o modelo canônico.
5. **Fim do épico:** mesclar os 2 deltas divergentes num único `DELTAS-...md` **v1.3** (Copiloto + Kits) — registrar tipos/providers/permissões de PRD-034-035 E manter a seção 3.19 (PRD-011 Copiloto) e os tipos do PRD-025.

## ❓ Decisões pendentes

- **Caminho para iniciar o PRD-035** — DECIDIDO: usuário mergeia o PR #25 primeiro; só então eu começo o PRD-035 a partir da `main` (porque o 035 mexe em código já mergeado). Sem ação até o merge.
- **Placeholder de seed por IA** — DECIDIDO: fica **só no PRD-035** (editor de kit, RF-016); o PRD-034 não recebe botão de IA. PR #25 permanece como aprovado.
- Nenhuma decisão em aberto bloqueando.

## 🚧 Bloqueios / Riscos

- **PRD-035 depende do merge do PR #25** — ele consolida/migra código que ainda está na branch do PRD-034. Branquear o 035 antes do merge geraria divergência. Aguardar merge.
- **Tag `v0.63.0` ainda local** — se o PR for mergeado por **squash**, o commit `42b7cca` não existirá na `main`; a tag precisa ser re-apontada. Não empurrar a tag antes de saber o tipo de merge.

## ⚠️ Avisos do usuário (regras desta sessão)

- **UI é validada MANUALMENTE pelo usuário** — NÃO abrir browser/devtools/preview. Dev server roda na porta 5173 (gerenciado pelo usuário; regenera `routeTree.gen.ts`).
- **Subagentes NÃO trocam de branch** (nem git checkout/switch/stash de branch).
- **IGNORAR completamente** qualquer pasta cujo caminho contenha `worktrees`.
- **Antes de remover/sobrescrever arquivos, confirmar que são idênticos.**
- **Commits na `main` exigem confirmação** (regra global). Trabalho desta sessão foi todo em branch de feature + PR.
- Conventional Commits em inglês, terminando com `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Responder em português do Brasil com acentuação correta.
- Não instalar dependências / test runner sem confirmar (guard 24h de supply-chain em `bunfig.toml`).

## 🧪 Gates do repositório (CRÍTICO para o próximo Claude)

- **`bun run build` NÃO type-checa** (esbuild). Gate de tipos real: `bunx tsc --noEmit 2>&1 | grep -iE "<arquivos tocados>"` deve sair VAZIO (ignorar ~316 erros pré-existentes não relacionados).
- **`bun run lint` global é INUTILIZÁVEL** (milhares de falsos `prettier/prettier Delete ␍` por CRLF). Validar prettier POR-ARQUIVO: `bunx prettier --check "<arquivo>"`.
- **Sem test runner.** Lógica pura → script descartável `scripts/_check_*.ts` rodado com `bun`, APAGADO no mesmo commit.
- **`src/routeTree.gen.ts` é GERADO** (nunca editar à mão; CRLF falso-positivo → `git checkout -- src/routeTree.gen.ts`).
- **Tokens semânticos APENAS** (sem hex/cores Tailwind cruas). Light + dark.

## 🛡️ Não regredir (features que devem continuar funcionando)

- **"Kits de revisão" (`IServiceKit`)** em `/app/catalogo/kits` — CRUD completo nas 3 cascas UX; permanece **intacto** até a consolidação do PRD-035. O menu mostra AMBOS "Kits de revisão" e "Kits por modelo".
- **`KitPicker`/`expandKitToItems`** no editor de orçamento (insere peças de um kit).
- O novo catálogo de modelos em `/app/kits` (lista por marca → detalhe com slot de kit vazio; Vendedor em leitura).

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/superpowers/specs/2026-06-03-catalogo-modelos-prd034-design.md` — spec do PRD-034 (decisões, IA de info, transição/consolidação).
- `docs/superpowers/plans/2026-06-03-catalogo-modelos-prd034.md` — plano executado (referência de padrões exatos do repo).
- `docs/prds/PRD-035-kits-composicao.md` (untracked) — PRD do próximo sub-projeto.
- `docs/prds/DELTAS-PRDs-Gallo-Base-Diesel (1).md` (untracked) — deltas do épico de Kits.
- `src/features/service-kits/**` — a feature de kit ATUAL a ser consolidada no PRD-035.
- `src/features/vehicle-models/**` — a feature entregue nesta sessão (base dos kits).
- `src/features/quotes/components/new/items/KitPicker.tsx` + `src/features/quotes/utils/kitExpansion.ts` — integração do kit no orçamento (será atualizada no 035).
- `CLAUDE.md` — convenções do projeto (versão atual: Catalog v0.63.0).

## 🧠 Memórias relacionadas

- `feedback_manual_testing` — usuário testa UI manualmente; não abrir browser/preview.
- `project_git_autocrlf_subagents` — CRLF falso-positivo (usar git cat-file); subagentes não trocam de branch.
- `project_goals_autostatus_bug` — bug fora de escopo (não relacionado a este épico).

## 📚 Referências

- PR: https://github.com/edmilson-prog/gallo-basediesel/pull/25
- Spec: `docs/superpowers/specs/2026-06-03-catalogo-modelos-prd034-design.md`
- Plano: `docs/superpowers/plans/2026-06-03-catalogo-modelos-prd034.md`
- Checkpoint anterior: `docs/checkpoints/2026-06-03-1034-kits-revisao-e-limpeza-prds.md`
