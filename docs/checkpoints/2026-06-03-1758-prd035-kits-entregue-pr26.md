# Checkpoint — PRD-035 Kits por Modelo entregue (PR #26) — 2026-06-03T17:58-03:00

> **Branch:** `feat/prd-035-kits-composicao` · **Último commit:** `df8506e` docs(prd-035): drop superseded PRD path after rename to _DONE
> **Sessão anterior:** Claude Opus 4.8 (1M context) · **Gerado em:** 2026-06-03T17:58:23-03:00

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-06-03-1758-prd035-kits-entregue-pr26.md` na íntegra e confirme em uma frase que entendeu:
1) o objetivo da sessão, 2) o estado atual do código, 3) qual é a próxima tarefa.
Não faça nenhuma ação até eu autorizar.

PR relacionado: https://github.com/edmilson-prog/gallo-basediesel/pull/26
```

---

## 📌 Contexto do projeto

**GALLO BASE DIESEL** — SaaS de inteligência comercial para distribuidora de peças de caminhão pesado (Volvo/Scania/Mercedes-Benz/Ford Cargo/Iveco), Fase 1 (Frontend First, dados mock). Stack: React 19 + TS strict, Vite, TanStack Router (file-based, `routeTree.gen.ts` GERADO) + TanStack Query, Tailwind v4 + shadcn/ui (new-york), Iconify `mdi:*`, zod + react-hook-form, sonner, Bun. Provider Pattern (`src/providers/data/`), mocks (`src/mocks/`), features (`src/features/`). Módulo desta sessão: **Kits por modelo** (`src/features/model-kits/`).

## 🎯 Objetivo da sessão

Implementar o **PRD-035 (Kits de Composição por Modelo)** — sub-projeto **2 de 3** do épico "Composição por Modelo (Kits)". Entregar o conceito de **Kit** (composição curada de peças, foco em filtros) pendurado no modelo canônico do PRD-034, gerenciado no detalhe do modelo e aplicável com um clique no orçamento, **consolidando** (recomeço limpo) a feature antiga `IServiceKit`. Fluxo: brainstorm → spec → plano → execução Subagent-Driven (20 tasks) → revisão holística → versão v0.64.0 → PR.

## ✅ Progresso (o que foi feito)

- [x] **Brainstorm + spec** aprovado — `docs/superpowers/specs/2026-06-03-kits-composicao-prd035-design.md` (commit `8df0b5b`). Consultoria de design (agente design-specialist) embutida. 4 decisões de produto: recomeço limpo; curadoria via filtro na lista de modelos; editor em **página dedicada**; undo>confirmação.
- [x] **Plano** de 20 tasks — `docs/superpowers/plans/2026-06-03-kits-composicao-prd035.md` (commit `8df0b5b`).
- [x] **20 tasks implementadas** via subagent-driven (commits `1f09076..5627e72`):
  - Fundações: tipos `IVehicleModelKit`/`IKitItem` (`1f09076`), `IQuote.appliedKitIds` (`6b40a5a`), seed 10 kits filtros (`633fccf`), mock api (`8dcc830`), provider slice (`25a8e8b`).
  - Lógica/RBAC: resource `modelKit` (`584f97a`), `findKitsForVehicle`/`getCompatiblePartsNotInKit`/`buildKitPreview` (`2378cf7`).
  - Gestão: hooks+validação (`848330a`), cards/badges/preview (`d3dc165`), seção no detalhe do modelo (`d8b3f7d`), editor página + rotas (`4fa977e`), banner drift (`06bf2d8`), curadoria na lista (`9c8fb95`).
  - Orçamento: modal preview `ApplyKitDialog` (`afdf62e`), fiação no QuoteEditor com snapshot+undo+appliedKitIds (`911a14c`), sugestão automática (`0c874a7`).
  - Veículo/cutover/versão: card "Filtros" + reader applyKitId (`fd44e39`), **cutover** removendo `service-kits` (`f58b81a`), release v0.64.0 "Kit" + tag (`5627e72`).
- [x] **Revisão holística (opus) = READY_TO_MERGE** — gate de tipos da feature vazio; build prod exit 0 (`✓ built in 18.41s`); cutover 100%; sem regressões; só tokens semânticos, zero `any`, sem nested-interactive.
- [x] **PR #26 aberto** (stacked, base `feat/prd-034-catalogo-modelos`): https://github.com/edmilson-prog/gallo-basediesel/pull/26
- [x] Limpeza pós-rename do PRD → `_DONE` (commit `df8506e`).
- [x] **Análise:** esclarecido que **PRD-016 já está implementado**; o que falta é o **delta** (ver Pendências).

## 🔧 Estado do código

- **Branch:** `feat/prd-035-kits-composicao` (empilhada sobre `feat/prd-034-catalogo-modelos`; ambas à frente de `origin/main` em `1ab5f79`).
- **Último commit:** `df8506e`. Os 19 commits de implementação + spec/plano (`8df0b5b`) + rename cleanup.
- **Tag:** `v0.64.0` "Kit" — **LOCAL apenas, NÃO pushada** (aguarda merge à main para publicar/reapontar se houver squash).
- **Feature nova:** `src/features/model-kits/` (utils, hooks, components, pages, index).
- **Build/tipos:** `bunx tsc --noEmit` filtrado pelos arquivos da feature = **vazio** (≈315 erros pré-existentes não relacionados, aceitáveis). `bun run build` = **exit 0**. Sem test runner.
- **PRs abertos relacionados:** **#26** (este, PRD-035) e **#25** (PRD-034, ainda OPEN, não mergeado).
- **Working tree:** limpo, exceto 1 input não-meu (`docs/prds/PRD-056-forecast-fechamento.md`, untracked — NÃO commitar, é insumo).

## ⏳ Pendências (próximos passos, em ordem)

1. **(Ação do usuário) Mergear o PR #25 (PRD-034).** Depois disso, eu: (a) **rebaseio `feat/prd-035` na `main`** (o PR #26 re-aponta para main automaticamente); (b) **publico a tag `v0.64.0`** (`git push origin v0.64.0`, ou reaponto se o merge for squash). Critério de feito: PR #26 com base `main`, tag no remoto.
2. **(Mim, 3º sub-projeto) Delta do PRD-016** — só as **3 entradas do épico de Kits** (seção 3.7 do arquivo de deltas). PRD-016 (Veículos) JÁ está implementado; isto é a extensão:
   - **(a)** Adicionar **`modelId: ID | null`** a `IVehicle` (`src/shared/types/customer.ts`), mantendo `brand/model/engine` como display denormalizado; vincular os ~60 veículos mock ao `modelId` na consolidação do PRD-034; UI "modelo não catalogado" → Gestor vincula/cria.
   - **(b)** Trocar **matching por string → `modelId`** dentro de `findKitsForVehicle` (`src/features/model-kits/utils/modelKitMatching.ts` — encapsulado de propósito para troca drop-in).
   - **(c)** Seção "**Peças compatíveis**" no **detalhe do veículo** refletir as `applications` do modelo canônico + indicar o Kit aplicável (drift). Substitui o placeholder "PRD-030 (placeholder)".
   - Já parcialmente feito no PRD-035: card "Filtros" navega ao orçamento com `applyKitId`; lógica de drift existe.
   - Fazer via brainstorm → spec → plano → subagent-driven (mesmo rito). Critério: `IVehicle.modelId` tipado+semeado, matching por id, seção peças compatíveis funcional, `tsc` filtrado vazio, versão MINOR/PATCH + tag.
3. **(Fim do épico) Mesclar os dois deltas divergentes** num documento canônico **v1.3**: `DELTAS-PRDs-Gallo-Base-Diesel.md` (Copiloto/PRD-025) + `DELTAS-PRDs-Gallo-Base-Diesel (1).md` (Kits/PRD-034-035). Hoje nenhum é superconjunto do outro.

## ❓ Decisões pendentes

- **Começar o brainstorm do Delta PRD-016 agora vs. mergear PRs #25/#26 primeiro?** — Usuário foi perguntado, ainda não respondeu (a sessão foi encerrada com checkpoint). Inclinação: como o Delta PRD-016 depende de PRD-034 **e** PRD-035, e ambos estão em PR empilhado não-mergeado, faz sentido **mergear #25 (e #26) primeiro** para o delta sair de uma `main` limpa — mas dá para brainstmar antes (não toca código). Decisão do usuário.

## 🚧 Bloqueios / Riscos

- **PR #25 (PRD-034) não mergeado** → `feat/prd-035` é stacked; até o merge, qualquer PR contra `main` mostraria também o diff do 034. Mitigado: PR #26 tem base `feat/prd-034`. Risco: se o #25 for mergeado via **squash**, será preciso rebase manual e reapontar a tag `v0.64.0`.
- **Matching por string** (limitação intencional do PRD-035) — sugestão automática e card "Filtros" casam por `brand+model` textual até o Delta PRD-016 ligar `modelId`.

## ⚠️ Avisos do usuário (regras desta sessão)

- **Usuário valida a UI manualmente** — NÃO abrir browser/devtools/preview para validar.
- **Subagentes NÃO trocam de branch** (sem `git checkout`/`stash` de branch).
- **IGNORAR completamente** qualquer pasta cujo caminho contenha `worktrees`.
- **Antes de remover/sobrescrever arquivo**, confirmar que é o esperado (ler primeiro).
- **Commits na `main` exigem confirmação** explícita.
- **24h supply-chain guard** (`bunfig.toml`) — NÃO adicionar dependências sem confirmar.
- Responder em **português do Brasil com acentos corretos**; **Conventional Commits** em inglês terminando com `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Inputs não-commitar: `docs/prds/DELTAS-*`, PDFs em `docs/reports/`, `docs/export/`, `docs/prds/PRD-056-*`, `delta-escopo-erp-gallo.md`.

## 🛡️ Não regredir (features que devem continuar funcionando)

- **Fluxo manual do editor de orçamento** (`QuoteEditor`): adicionar peça, item livre, swap de equivalente — intactos; kit é aditivo.
- **Catálogo de modelos (PRD-034)**: lista agrupada por marca + detalhe `/app/kits/$modelId` (agora com seção de kits).
- **Redirect** de `/app/catalogo/kits` → `/app/kits` (a feature antiga "Kits de revisão" foi REMOVIDA no cutover; o endereço antigo redireciona).
- **Matriz RBAC** `RESOURCE_LABELS` exaustiva (`Record<ResourceName,string>`) — `serviceKit` removido, `modelKit` presente.
- **Aplicação de kit no orçamento** com snapshot de preço/OEM no `IQuoteItem`.

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/superpowers/specs/2026-06-03-kits-composicao-prd035-design.md` — spec do PRD-035 (decisões, 4 superfícies, limitações).
- `docs/superpowers/plans/2026-06-03-kits-composicao-prd035.md` — plano de 20 tasks (gates do repo no topo).
- `docs/prds/DELTAS-PRDs-Gallo-Base-Diesel (1).md` **seção 3.7 (linhas ~179-191)** — o **Delta PRD-016** (próxima tarefa).
- `src/features/model-kits/` — a feature entregue (utils/hooks/components/pages).
- `src/shared/types/customer.ts` — `IVehicle` (onde entra `modelId` no delta).
- `src/features/model-kits/utils/modelKitMatching.ts` — `findKitsForVehicle` (trocar string→modelId no delta).
- `src/features/vehicles/components/detail/MaintenanceRecommendations.tsx` — cards do veículo.
- `CLAUDE.md` — convenções; `CHANGELOG.md` — v0.64.0 "Kit".

## 🧠 Memórias relacionadas

- `project_git_autocrlf_subagents` — CRLF aparente é falso positivo (use `git cat-file`); subagentes não trocam de branch. **Gate nuance descoberto:** `prettier --check` em arquivo PRÉ-EXISTENTE acusa warning por CRLF; se `prettier --write` não gera diff de conteúdo, é falso positivo, seguir.
- `feedback_manual_testing` — usuário testa UI manualmente; não abrir browser/preview.
- `project_goals_autostatus_bug` — bug conhecido fora de escopo.

## 📊 Atividade recente (telemetria)

Sem `.claude-metrics/annotations.jsonl` ativo nesta sessão.

## 📚 Referências

- Spec: `docs/superpowers/specs/2026-06-03-kits-composicao-prd035-design.md`
- Plano: `docs/superpowers/plans/2026-06-03-kits-composicao-prd035.md`
- PR (PRD-035): https://github.com/edmilson-prog/gallo-basediesel/pull/26
- PR (PRD-034, pré-requisito): #25 (OPEN)
- PRD: `docs/prds/PRD-035-kits-composicao_DONE.md` (Status ✅ IMPLEMENTADO, v0.64.0)
- Épico/cadeia: PRD-034 ✅ (PR #25) → PRD-035 ✅ (PR #26) → Delta PRD-016 ⏳
