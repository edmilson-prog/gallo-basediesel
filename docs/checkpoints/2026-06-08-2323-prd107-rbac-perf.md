# Checkpoint — PRD-107 Fase 3 + RBAC fino (Slices 3/4) + Perf PRD-108 — 2026-06-08T23:23-03:00

> **Branch:** `feat/fase2-supabase-cutover` · **Último commit:** `31d4702` docs(db): document PRD-108 perf pass
> **Sessão anterior:** Claude Opus 4.8 · **Gerado em:** 2026-06-08T23:23-03:00

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-06-08-2323-prd107-rbac-perf.md` na íntegra e confirme em
uma frase que entendeu: 1) o objetivo da sessão, 2) o estado atual do código, 3) qual é a próxima
tarefa. Não faça nenhuma ação até eu autorizar.

PR relacionado: https://github.com/edmilson-prog/gallo-basediesel/pull/39
```

---

## 📌 Contexto do projeto

GALLO BASE DIESEL — SaaS de inteligência comercial (Vite + React 19 + TS strict, TanStack Router/Query, Zustand, Tailwind v4/shadcn). Estamos na **Fase 2 (Mock → Supabase)**, branch de longa duração `feat/fase2-supabase-cutover` consolidada na **draft PR #39 (NUNCA mergear até a Fase 2 fechar)**. O backend já está materializado (33 data-providers + subsistema de notificações, 39 tabelas schema `public` com RLS, auth real switchável `VITE_AUTH_SOURCE=mock|supabase`). O **Custom Access Token Hook está habilitado** → o JWT carrega `app_metadata` (role/seller_id/store_id). App roda em `supabase` para o owner logado; default segue `mock`. Projeto ref Supabase: `njizaasajkdqptlxddqn`.

## 🎯 Objetivo da sessão

Avançar a Fase 2 em três frentes, sempre via protocolo AILA (explore→plano→"confirma"):
1. **Concluir PRD-107 Fase 3 (gestão de usuários)** — faltavam **redefinir senha** e **trocar papel** (criar acesso e desligar/reativar já existiam).
2. **Completar o isolamento per-seller (RLS)** — Slices 3 e 4, fechando todas as tabelas com dono.
3. **Perf PRD-108 (core)** — índices de FK + initplan da `profiles`.

## ✅ Progresso (o que foi feito) — 6 commits, 2 Edge Functions, 4 migrations

- [x] `ab76ef6` **feat(admin): reset de senha** — Edge Function `reset-seller-password` (v1, owner/staff, guards espelham set-seller-access) + `ResetPasswordDialog.tsx` + `resetSellerPassword()` em `sellerAccess.ts`. Funciona com vendedor banido. **Aviso UX:** dialog alerta quando o vendedor está desligado.
- [x] `a79232d` **feat(admin): trocar papel** — Edge Function `set-seller-role` (v1, **owner-only**) + `ChangeRoleDialog.tsx`. Atualiza `profiles.role` e sincroniza `sellers.type` (manager mantém o tipo). Vale no próximo login do alvo. Badge "Gestor" na lista; ação escondida pro próprio owner.
- [x] `8372b98` **style(settings): largura** — `SettingsLayout` de `max-w-4xl`→`max-w-6xl` (compartilhado por todas as telas de Configurações). Validado visualmente pelo usuário.
- [x] `b43e46f` **RLS Slice 3 — assets pessoais** — migration `rls_slice3_personal_assets`. `quick_replies`/`asset_combos` isolados por `owner_id` (SELECT de quick_replies também expõe `scope='shared'`). Validado: Lucas 8 quick_replies / 0 combos vs owner 20 / 5.
- [x] `3c0749c` **RLS Slice 4 — derivadas per-seller** — migration `rls_slice4_personal_derived`. `customer_segments` (com `scope='shared'`), `asset_favorites`, `asset_send_log` (bucket derivado→sellers, sem store_id). Validado por impersonação + teste de injeção (tx revertida): segmento privado de outro vendedor fica oculto.
- [x] `31d4702` **Perf PRD-108 core** — migrations `perf_index_unindexed_fks` (21 índices de FK) + `profiles_select_consolidate_initplan` (as 2 policies permissivas de SELECT na profiles viraram 1, com `auth.*` em `(select …)`). Advisor: `unindexed_foreign_keys` 21→0, `auth_rls_initplan` 2→0, `multiple_permissive` 1→0; segurança inalterada.

> Isolamento per-seller **Slices 1–4 completo** em todas as tabelas com dono. Toda a documentação RLS vive em `docs/db/rls-policies-fase2-mvp.md`.

## 🔧 Estado do código

- **Branch:** `feat/fase2-supabase-cutover` (18 commits à frente da `main`; 6 desta sessão sobre o checkpoint anterior `30e7f67`).
- **Último commit:** `31d4702`.
- **Working tree:** só ruído — `M src/routeTree.gen.ts` (gerado, NÃO commitar) + untracked `docs/relatorio-codigo-morto-2026-06-04.md` e `knip.json` (pré-existentes, não são desta sessão).
- **Build/testes:** `bun run build` PASS e `bun run test` 244/244 na última feature de código (trocar papel). Slices 3/4 + perf são **só banco** (sem código → sem gate). `tsc` delta limpo nos arquivos novos.
- **Migrations remotas aplicadas (MCP):** `rls_slice3_personal_assets`, `rls_slice4_personal_derived`, `perf_index_unindexed_fks`, `profiles_select_consolidate_initplan`.
- **Edge Functions deployadas (MCP):** `reset-seller-password` (v1), `set-seller-role` (v1) — sources em `supabase/functions/` (não há pasta `supabase/migrations` local; migrations são remotas).
- **PR aberto:** #39 (draft) — `feat(fase2): Supabase cutover`.

## ⏳ Pendências (próximos passos, em ordem)

1. **Storefront anon** — políticas de leitura anônima do catálogo B2C (loja pública sem login). Só READ; **anon WRITE é proibido**. Tabelas: catálogo (`parts`, etc.) + storefront. Critério: visitante anônimo lê catálogo, resto fechado; `get_advisors(security)` sem novos erros.
2. **Part C (perf proativo)** — envelopar `current_*()`/`is_staff()` em `(select …)` nas ~140 policies restantes (37 tabelas × 4). Semanticamente idêntico; advisor NÃO flaga (são funções wrapper, não `auth.*` direto). Gerar DDL com cuidado + revalidar por impersonação. Critério: contagens por impersonação inalteradas.
3. **Remover fallback `profiles` das helpers** — o hook já é universal, então `current_*()` pode ler só o JWT (perf). Arquivos: helpers via migration. Critério: impersonação sem `app_metadata` deixa de funcionar (esperado) e com claims funciona.
4. **Convite por email (PRD-141 / Resend)** — substituir senha temporária por magic link no `invite-seller`.
5. **pgTAP + workflow CI** (`rls-tests.yml`) — testes de RLS versionados.
6. **Pool de não-atribuídos** — semântica de conversas sem dono (quem vê o pool).

## ❓ Decisões pendentes

- **Qual frente seguir a seguir?** O usuário vinha escolhendo uma por vez. Inclinação: **Storefront anon** (abre superfície nova) ou **Part C** (consolida perf). Sem decisão travada.
- **Part C agora ou depois?** Deferido nesta sessão (risco/tamanho); o usuário pode pedir o pass completo.

## 🚧 Bloqueios / Riscos

- **Criar `auth.users` via SQL do agente com credenciais inventadas é BLOQUEADO pelo classificador.** Usar SEMPRE o fluxo da plataforma (Edge Functions `invite-seller`/`reset-seller-password` que o owner dispara).
- `asset_favorites`/`asset_send_log` estão **vazios no seed** — isolamento provado por construção, não por dado.

## ⚠️ Avisos do usuário (regras desta sessão — preservar)

- **PR #39 fica DRAFT; NÃO mergear até a Fase 2 fechar.**
- **"muito cuidado com regressões."**
- **`service_role` NUNCA no cliente** — vive só em `.env.local` (sem prefixo `VITE_`, gitignored, para o seeder; **não ler o valor**). Edge Functions pegam do env do servidor automaticamente.
- **Abrir policy de WRITE anônima é PROIBIDO.**
- **Ignorar `.claude/worktrees/` por completo.**
- **O usuário testa a UI manualmente** (cola erros do console) — **NÃO abrir browser/preview**.
- `src/routeTree.gen.ts` é ruído gerado — **nunca commitar**. Avisos CRLF são falso-positivo.
- `bunfig.toml` impõe guard de 24h supply-chain — **confirmar antes** de mexer em `minimumReleaseAgeExcludes`.
- **Protocolo AILA pre-task:** explorar → plano → aguardar **"confirma"** antes de implementação substantiva.
- Responder sempre em **pt-BR com acentuação correta**.
- Trailer de commit: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 🛡️ Não regredir (deve continuar funcionando)

- **Isolamento per-seller (Slices 1–4):** staff (owner/manager) vê tudo; não-staff vê só a própria carteira (clientes/pedidos/orçamentos/leads/conversas), financeiras ocultas, e só seus assets/segmentos/favoritos (+ `shared` em quick_replies/segments).
- **Gestão de usuários (tela Usuários, owner):** criar acesso, redefinir senha, desligar/reativar, trocar papel — todos gated.
- **`profiles` SELECT consolidado** (`profiles_select_self_or_staff`): owner lê os profiles da loja (`listSellerAccessRoles`), vendedor lê o próprio; fallback das helpers segue ok.
- **Notificações:** recipient do gestor via `stores.manager_id` (link owner↔seller).
- **Auth switchável** (`VITE_AUTH_SOURCE`); hook de JWT habilitado.

## 🔑 IDs verificáveis (impersonação por `set_config('request.jwt.claims', …)`)

- **Owner Fernando:** auth_user_id `57706ecc-…` · store matriz `00000000-0000-0000-0000-000000000001` · role `owner` (seller_id = SEED_OWNER_ID).
- **Lucas (vendedor de teste):** auth_user_id `154c3c64-15c0-41ec-824c-9fbfc3cc9ac4` · seller_id `5a6400ed-5aec-4bf1-b641-31635f15c887` · role `seller_internal`.
- **Supabase project ref:** `njizaasajkdqptlxddqn`.
- **Helpers RLS:** `current_store_id()`, `current_seller_id()`, `current_app_role()`, `is_staff()` — todas `sql stable security invoker set search_path=''`, lendo `auth.jwt()->'app_metadata'` com fallback `profiles`.
- **Padrão per-seller:** `store_id = current_store_id() AND (is_staff() OR <col> = current_seller_id())` (derivadas usam `<col> in (select id from sellers where store_id = current_store_id())` como store-scope).

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/db/rls-policies-fase2-mvp.md` — **a bíblia de RLS** (Slices 1–4 + perf, com migrations, predicados e validações).
- `src/features/admin-settings/api/sellerAccess.ts` — client das 4 ações (invite/reset/setAccess/setRole) + `listSellerAccessRoles`.
- `src/features/admin-settings/components/{CreateAccess,ResetPassword,ChangeRole,ToggleSellerAccessButton}` + `pages/UsersPlaceholderPage.tsx`.
- `supabase/functions/{invite-seller,set-seller-access,reset-seller-password,set-seller-role}/index.ts`.
- `src/features/shell/layouts/SettingsLayout.tsx` — layout/largura das Configurações.
- `CLAUDE.md` — convenções do projeto.

## 🧠 Memórias relacionadas

- `project_fase2_supabase_kickoff.md` — kickoff da Fase 2 (backend, RLS, convenções).
- `feedback_manual_testing.md` — o usuário testa a UI manualmente.
- `project_tsc_baseline_errors.md` — gate real é `bun run build`; avaliar código novo por delta.
- `project_routetree_merge_block.md` — `routeTree.gen.ts` suja a working tree.

## 📚 Referências

- PR: https://github.com/edmilson-prog/gallo-basediesel/pull/39 (draft)
- Checkpoint anterior: `docs/checkpoints/2026-06-08-2155-prd107-rbac-usuarios.md`
- PRDs: PRD-103 (RLS), PRD-107 (Auth/identidade), PRD-108 (perf), PRD-141 (email).
