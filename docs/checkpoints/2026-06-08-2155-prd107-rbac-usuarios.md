# Checkpoint — PRD-107 (Auth/JWT) + RBAC fino + Gestão de Usuários — 2026-06-08T21:55-03:00

> **Branch:** `feat/fase2-supabase-cutover` · **Último commit:** `9b3d732` feat(users): deactivate/reactivate seller access
> **Sessão anterior:** Claude Opus 4.8 · **Gerado em:** 2026-06-08T21:55-03:00 · **PR:** #39 (draft)

---

## 🎯 Como retomar (cole isto na nova sessão)

```
Leia o arquivo `docs/checkpoints/2026-06-08-2155-prd107-rbac-usuarios.md` na íntegra e confirme
em uma frase que entendeu: 1) o objetivo da sessão, 2) o estado atual do código, 3) qual é a
próxima tarefa. Não faça nenhuma ação até eu autorizar.

PR relacionado: https://github.com/edmilson-prog/gallo-basediesel/pull/39
```

---

## 📌 Contexto do projeto

**GALLO BASE DIESEL** — SaaS de inteligência comercial (Vite + React 19 + TanStack Router/Query + Tailwind v4/shadcn + Zustand). Fase 2 = migração Mock → **Supabase** (project ref `njizaasajkdqptlxddqn`, schema `public`, 39+ tabelas com RLS). Provider Pattern (`@/providers/data`, 33 providers) + subsistema de notificações (`@/providers/notifications`), ambos switchados por `VITE_DATA_SOURCE`/`VITE_AUTH_SOURCE` (build-time). Migrations e Edge Functions são aplicadas **via MCP Supabase** (não há `supabase/migrations/` local; **há** `supabase/functions/` versionado). App default segue `mock`; o ambiente de teste roda `AUTH=supabase + DATA=supabase`.

## 🎯 Objetivo da sessão

Avançar o cutover da Fase 2 fechando o **ciclo de identidade e segurança**: (1) **PRD-107 Fase 1** — identidade real via claims no JWT; (2) **PRD-103 RBAC fino** — isolamento de carteira por vendedor + financeiras staff-only; (3) **PRD-107 Fase 3** — gestão de usuários **pela plataforma** (criar acesso, desligar/reativar), porque o classificador (corretamente) bloqueou criar `auth.users` via SQL com credenciais inventadas pelo agente. Meta-diretriz do usuário: **"implementa o que falta, depois vamos corrigindo"**, tudo numa **PR draft de longa duração (#39), nunca mergeada até concluir**.

## ✅ Progresso (o que foi feito — 7 commits desta sessão)

- [x] `70d7476` — **PRD-107 Fase 1**: 4 helpers de RLS leem identidade do **JWT `app_metadata`** com **fallback** para subquery em `profiles` (migration MCP `rls_helpers_jwt_claims_with_fallback`). Cutover sem janela de lockout. **Usuário habilitou o Custom Access Token Hook no Dashboard** → claims reais confirmadas no token (`app_metadata` com role/store/seller_id).
- [x] `4ee11b8` — **fix**: reconciler de notificações descarta recipient nulo (bug do `recipient_id` NOT NULL → `400` no Supabase; `store.managerId` não existia em `IStore`).
- [x] `873dbb4` — **vínculo owner↔seller**: `IStore.managerId?` + migration `add_manager_id_to_stores` + `stores.manager_id` e `profiles.seller_id` do owner = **Fernando** (`57706ecc-…`). Alertas de gestor voltam ao sino.
- [x] `216a8a5` — **RBAC fino Slice 1** (doc): migration `rls_per_seller_carteira_scope` — `customers/orders/quotes/leads/commissions.seller_id` + `conversations.assigned_seller_id` → `store AND (is_staff() OR <col>=current_seller_id())` nos 4 comandos. Filhas herdam.
- [x] `968e0de` — **PRD-107 Fase 3 (criar acesso)**: Edge Function **`invite-seller`** (service_role server-side, gate owner) + migration `profiles_select_staff` + tela `Usuários` funcional. + fix reconciler `customer.ownerId`→`sellerId`.
- [x] `42aaa88` — **RBAC fino Slice 2** (doc): migration `rls_slice2_financial_staff_only` — `expenses/cash_flow_entries/distribution_traces` = **staff-only**; `goals/recommendations/product_indicators` = per-seller.
- [x] `9b3d732` — **PRD-107 Fase 3 (desligar/reativar)**: Edge Function **`set-seller-access`** (ban/unban + flip `sellers.active`) + `ToggleSellerAccessButton` (AlertDialog).
- [x] **Validações por impersonação** (todas ✅): JWT path vs fallback; Lucas vê 18/132/10/28 + 0 financeiras; owner vê tudo; vazamento cruzado = 0. **Usuário criou o login do Lucas pela UI e logou** — sidebar reduzida confirmou a guarda de rota por papel.

## 🔧 Estado do código

- **Branch:** `feat/fase2-supabase-cutover` (11 commits à frente da `main`; 7 desta sessão) · **upstream OK, tudo pushado**.
- **Último commit:** `9b3d732`.
- **Working tree:** só **ruído** — `M src/routeTree.gen.ts` (gerado, NÃO commitar) + untracked `docs/relatorio-codigo-morto-2026-06-04.md` e `knip.json` (pré-existentes).
- **Arquivos desta sessão (delta vs main):**
  - `supabase/functions/invite-seller/index.ts` (A) — cria acesso (createUser + link profile).
  - `supabase/functions/set-seller-access/index.ts` (A) — ban/unban + flip active.
  - `src/features/admin-settings/api/sellerAccess.ts` (A) — `inviteSeller`, `setSellerAccess`, `listSellerAccessRoles`, `generateTempPassword`.
  - `src/features/admin-settings/components/CreateAccessDialog.tsx` (A) · `ToggleSellerAccessButton.tsx` (A).
  - `src/features/admin-settings/pages/UsersPlaceholderPage.tsx` (M) — tela funcional (criar/desligar/reativar, gated a supabase).
  - `src/providers/notifications/reconciler.ts` (M) — filtro de recipient nulo + `sellerId`.
  - `src/providers/data/impl/supabase/stores.ts` (M) — mapeia `manager_id`.
  - `src/shared/types/platform.ts` (M) — `IStore.managerId?`.
  - `src/mocks/data/seedStore.ts` (M) — `managerId` no mock (simetria).
  - `docs/db/rls-policies-fase2-mvp.md` (A nesta linha) — **a bíblia de RLS** (todas migrations, buckets, Slices, validações).
- **Artefatos remotos (MCP, sem arquivo local):** migrations `rls_helpers_jwt_claims_with_fallback`, `add_manager_id_to_stores`, `rls_per_seller_carteira_scope`, `profiles_select_staff`, `rls_slice2_financial_staff_only`; Edge Functions `invite-seller` (v1) e `set-seller-access` (v1) deployadas (sources NO repo); dados `stores.manager_id`/`profiles.seller_id`=Fernando; **hook habilitado no Dashboard** (ação do usuário).
- **Build/testes:** `bun run build` ✅ · `bun run test` **244/244** ✅ · `tsc --noEmit` delta limpo ✅ · `eslint` ✅ · smoke-tests das 2 Edge Functions ✅ (no-auth→401, anon→"invalid session").
- **PR aberto:** **#39** (draft) — não mergear.

## ⏳ Pendências (próximos passos, em ordem)

1. **(Usuário) Testar desligar/reativar** — relogar como `admin@ailainteligente.com` (owner) → Configurações → Usuários → "Desligar" o Lucas → confirmar que o login dele falha → "Reativar". A rota Usuários é só-Owner (Lucas não a vê). **Feito = toast OK + login do Lucas barrado/liberado.**
2. **Trocar papel do vendedor** (interno↔externo↔gestor) na tela Usuários — nova ação + provavelmente estender uma Edge Function (atualizar `profiles.role` + `sellers.type`). **Feito = papel muda e reflete no próximo refresh de token.**
3. **Assets pessoais** (`quick_replies`/`asset_combos.owner_id`) — último resíduo de RBAC fino (per-owner). Arquivos: migration de policy (ALTER POLICY). **Feito = vendedor vê só os próprios + compartilhados.**
4. **Storefront anon** — policies `anon` de catálogo p/ loja B2C pública em supabase. **Feito = `/loja` lê catálogo deslogado.**
5. **Perf (PRD-108)** — indexar FKs (20 `unindexed_foreign_keys`), envolver `current_*()`/`is_staff()` em `(select …)` p/ otimizar `auth_rls_initplan`. **Feito = advisors de performance limpos.**
6. **Convite por email (Resend/PRD-141)** — troca a senha temporária por magic link no `invite-seller`.
7. **pgTAP + workflow CI** (`rls-tests.yml`) para travar regressões de RLS.
8. **Remover o fallback `profiles`** das helpers quando o hook estiver universal (perf).

## ❓ Decisões pendentes

- **Próximo item a atacar** — entre #2 (trocar papel), #4 (storefront anon) e #5 (perf). Inclinação: nenhuma forte; é escolha do usuário. (As decisões de design já tomadas: gestor = Fernando; desligar = **banir** (reversível), não deletar; Slice 2 inclui per-seller além das financeiras.)

## 🚧 Bloqueios / Riscos

- **Criar `auth.users` via SQL do agente está BLOQUEADO** pelo classificador (credenciais inventadas). Caminho válido = fluxo da plataforma (Edge Functions) que o owner dispara. Já contornado para "criar acesso" e "desligar/reativar".
- **Abrir policies `anon` de escrita está PROIBIDO** (classifier-denied). Para storefront anon, são policies `anon` **de leitura** de catálogo (provavelmente OK), mas validar com cuidado.
- **Invalidação de sessão na desativação é no próximo refresh (~1h)**, não imediata (limitação MVP aceita).

## ⚠️ Avisos do usuário (regras desta sessão)

- **NÃO MERGEAR** — PR #39 fica **draft** até concluir a Fase 2; trabalho commitado na branch `feat/fase2-supabase-cutover`.
- **Muito cuidado com regressões.**
- **`service_role` NUNCA no cliente** — vive só no `.env.local` (sem prefixo `VITE_`, gitignored) para o seeder; Edge Functions recebem do ambiente automaticamente. **Não ler o valor da chave.**
- **Protocolo PRE-TASK (AILA):** explorar (read-only) → planejar → **aguardar "confirma"** antes de implementação substantiva (sobretudo RLS/auth).
- **Ignorar `.claude/worktrees/`** por completo.
- **Usuário testa a UI manualmente** (abre o browser, cola erros do console). **Não abrir browser/preview** para validar.
- **`src/routeTree.gen.ts`** é ruído gerado — **nunca commitar**.
- **CRLF warnings são falso-positivo** (git guarda LF).
- **bunfig 24h supply-chain guard** — confirmar antes de `minimumReleaseAgeExcludes`.
- **Responder em pt-BR** com acentuação correta.

## 🛡️ Não regredir (deve continuar funcionando)

- App em `AUTH=supabase + DATA=supabase` p/ o owner: **leitura + escrita + notificações** OK (banner de origem não aparece).
- **Identidade via JWT** viva (hook habilitado) — e helpers com **fallback** (funciona mesmo sem claims).
- **RBAC:** owner/staff vê a loja inteira; **vendedor vê só a própria carteira** (Slice 1) e **0 financeiras** (Slice 2). Owner inalterado.
- **Gestão de usuários:** "Criar acesso" (`invite-seller`) e "Desligar/Reativar" (`set-seller-access`) funcionam; **owner não pode ser desligado**; ninguém desliga o próprio acesso.
- **Mock mode intacto** e default (build-time).
- **244 testes Vitest** + build verdes.

## 📂 Arquivos-chave (ler primeiro na nova sessão)

- `docs/db/rls-policies-fase2-mvp.md` — **a fonte da verdade de RLS** (helpers, buckets, Slice 1/2, todas as migrations, validações por impersonação).
- `supabase/functions/invite-seller/index.ts` e `set-seller-access/index.ts` — padrão das Edge Functions (gate owner, service_role server-side).
- `src/features/admin-settings/pages/UsersPlaceholderPage.tsx` + `api/sellerAccess.ts` + `components/{CreateAccessDialog,ToggleSellerAccessButton}.tsx` — gestão de usuários.
- `src/providers/notifications/reconciler.ts` — reconciler (fix de recipient).
- `src/features/auth/authSource.ts`, `SupabaseAuthProvider.tsx`, `roleMap.ts` — auth real.
- `CLAUDE.md` — convenções.

## 🔑 IDs / fatos verificáveis

- **Owner:** `admin@ailainteligente.com` · auth_user_id `9a418578-2671-4141-a15a-d39b2fd13af7` · role `owner` · store matriz `00000000-0000-0000-0000-000000000001` · **profile.seller_id = Fernando** `57706ecc-01b5-4a96-b403-0359a4bb767f`.
- **Vendedor de teste:** Lucas Costa · seller_id `5a6400ed-5aec-4bf1-b641-31635f15c887` · role `seller_internal` · login `lucas@gallobasediesel.com.br` **criado pela UI** (senha temporária definida pelo usuário). Carteira: 18 clientes / 132 pedidos / 28 conversas.
- **Project ref:** `njizaasajkdqptlxddqn` · URL `https://njizaasajkdqptlxddqn.supabase.co`.

## 🧠 Memórias relacionadas

- `project_fase2_supabase_kickoff.md` — **a memória central da Fase 2** (atualizada nesta sessão com PRD-107 F1/F3, RBAC Slice 1/2, owner↔seller, fix do reconciler).
- `feedback_manual_testing.md` — usuário testa UI manualmente.
- `project_tsc_baseline_errors.md` · `project_routetree_merge_block.md` · `project_git_autocrlf_subagents.md` · `project_devserver_stale_branch.md`.

## 📚 Referências

- PR: https://github.com/edmilson-prog/gallo-basediesel/pull/39 (draft)
- Checkpoint anterior: `docs/checkpoints/2026-06-08-1955-fase2-rls-conv-notif.md`
- PRDs: `docs/prds/PRD-107-auth-custom-claims.md` (Fases 1+3 parciais feitas), `docs/prds/` (PRD-103 RLS).
