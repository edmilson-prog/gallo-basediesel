/**
 * GALLO BASE DIESEL — People & Access feature (PRD-211).
 *
 * Public surface for org-people management: departments (Departamentos — the
 * revived `ITeam`), and later the users list (Task 14). Consume via this barrel.
 */
export { DepartmentManager } from "./components/DepartmentManager";
export { DepartmentsPage } from "./pages/DepartmentsPage";
