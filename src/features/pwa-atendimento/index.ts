/**
 * GALLO Atendimento — PWA de conversas.
 *
 * Sub-app mobile instalável em `/atendimento`, só de troca de mensagens. Usa a
 * mesma sessão e os mesmos providers do SaaS; o recorte (sem assumir, sem
 * transferir, sem tags, sem notas) veio do kit `ui_kits/pwa-atendimento`.
 */
export { PwaShell } from "./components/PwaShell";
export { PWA_ATENDIMENTO_STRINGS } from "./i18n/pt-BR";
