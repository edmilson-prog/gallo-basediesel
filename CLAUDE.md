# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Projeto

**GALLO BASE DIESEL** — Plataforma SaaS de inteligência comercial para distribuidora de peças pesadas (Volvo, Scania, Mercedes-Benz, Ford Cargo, Iveco) em Frederico Westphalen/RS. A plataforma se posiciona **acima do ERP DINTEC** como cérebro comercial e relacional. Marca guarda-chuva com 3 submarcas: PARTS (verde), SERVICE (vermelho), INDUSTRIAL (amarelo).

Estado atual: **Fase 2 (Mock → Supabase) — escopo A concluído e mergeado na `main`** (PR #39, codinome `Keystone`, v0.73.0). A Fase 1 (Frontend First) entregou o app completo sobre dados fictícios determinísticos (faker + seed) na arquitetura-alvo (`features/`, `mocks/`, `providers/`, `shared/`); a maioria dos PRDs comerciais está `_DONE` — incluindo gestão de mídia (PRD-026, "Vault") e envio rápido/biblioteca de ativos (PRD-027, "Dispatch"). O **backend do cutover está pronto e validado**: 34 providers Supabase, 39 tabelas (schema `public`, todas com RLS — write policies e isolamento per-seller incluídos), seed populado, **auth real switchável** (`VITE_AUTH_SOURCE=mock|supabase`) com Custom Access Token Hook habilitado, 9 Edge Functions (gestão de usuários + canário `hello-trace` + healthcheck público `health` + webhook público `whatsapp-webhook` + envio `whatsapp-send`) e `pg_cron` recompondo notificações derivadas no servidor. Suíte de regressão de RLS versionada em `supabase/tests/rls-regression.sql` (workflow de CI no-op até existir o secret `SUPABASE_DB_URL`). A faixa de infra **PRD-100–110 está fechada**: PR #50 (v0.74.0 `Backbone`) entregou migrations **espelhadas em `supabase/migrations/`** (⚠️ regra: todo `apply_migration` via MCP deve ser exportado para o Git no mesmo PR), Edge Functions padronizadas em `_shared/`, Realtime core no Inbox, Storage com upload real e MVs de BI via RPCs scoped; PR #51 (v0.75.0 `Sentinel`) fechou **backup/DR e observabilidade** — workflows semanais de backup lógico/Storage (no-op até secrets), 4 runbooks de restauração + `dr-policy.md`, Sentry gated por env (frontend + edge, PII scrub, correlação por traceId), healthcheck público (`/functions/v1/health`) e dashboard `/app/gestao/saude` Owner-only (provider `systemHealth`, o 35º). 🚀 **GO-LIVE (2026-06-10): produção (crm.gallobasediesel.com.br) roda `supabase` em dados e auth** — flip #47 executado com smoke completo aprovado pelo dono (owner + vendedor + loja anônima), **PITR habilitado** no mesmo dia e `INVITE_REDIRECT_URL` de produção no Vault; hotfixes do smoke mergeados e deployados (**PR #65** — teto de listas 200→1000 em 17 providers supabase; **PR #66** — invalidação de queries no detalhe do cliente pós-mutação + espelho de sessão `gallo-auth-sync` com `sellerId` para o FK `audit_logs.actor_id` → sellers). Issues #45/#46/#47/#52 fechadas; pendência viva: 1º teste de DR (`docs/fase2-pendencias.md` §D1). O convite por e-mail (#46) está **ativo e validado e2e**. A **loja transacional B2C** (#40/#41) foi deferida para fase própria — em `supabase` o checkout faz handoff por WhatsApp (write-free). A **Onda 5 (WhatsApp real) está em andamento**: PR #53 (v0.76.0 `Bridge`) fechou os PRDs 111–113 (camada `src/providers/whatsapp/` — interface + mock + engines reais Meta/Evolution, runtime-agnostic com deps injetadas); PR #54 (v0.77.0 `Gateway`) entregou o webhook unificado `whatsapp-webhook` (recepção, fail-closed); PR #55 (v0.78.0 `Courier`) entregou o pipeline de envio `whatsapp-send` + `useMessageSend` ramificado por fonte; PR #56 (v0.79.0 `Stencil`) entregou o catálogo de templates HSM (`message_templates` + provider `messageTemplates`, o 36º, tela `/app/configuracoes/templates-whatsapp` e `TemplatePicker` integrado ao `TEMPLATE_REQUIRED`); PR #57 (v0.80.0 `Hourglass`) fechou a janela de 24h (engine puro `sessionWindow.ts`, RPC `last_inbound_at` invoker, CTA de template no banner fechado); PR #58 (v0.81.0 `Pulse`) fechou o status tracking (badges ao vivo via Realtime com anti-regressão de status, retry real, gate de número inválido `131026` com override staff auditado e RPC owner-only `whatsapp_delivery_health` na tela de saúde); PR #59 (v0.82.0 `Weave`) fechou a reconciliação com as telas Fase 1 (auditoria limpa — Inbox/Conversa/Distribuição já migradas; deltas: anexo de arquivo avulso real no composer via `useAttachmentUpload` e tela `WhatsAppAccountsPage` em Configurações → WhatsApp com edição staff-only de `provider_config`; docs em `docs/dev/onda5-migration.md`); PR #60 (v0.83.0 `Relay`) **fechou a Onda 5** com failover Meta↔Evolution + monitoring (colunas de failover em `whatsapp_accounts`, engine puro `failover.ts`, conta efetiva resolvida no `send/core.ts` com `FAILOVER_INCOMPATIBLE` para template em reserva Evolution, tick SQL `whatsapp_health_tick` via pg_cron */5 com notificações in-app, RPC owner-only `whatsapp_provider_health` + seção "Provedores & Failover" na tela de saúde, override manual auditado; docs em `docs/dev/whatsapp-failover.md`). **Onda 5 COMPLETA** — e2e real segue gated nas credenciais Meta/decisão VPS Evolution. **Onda 6 (DINTEC) encerrada por decisão (2026-06-10):** PR #62 (v0.84.0 `Anchor`) entregou o PRD-121 (camada `src/providers/dintec/` — interface agnóstica de fonte + mock determinístico); os PRDs 122–126 foram **deferidos** — o DINTEC não disponibiliza export e as importações CSV, quando ocorrerem, serão conduzidas de forma assistida pelo agente desenvolvedor (dry-run + revisão), sem tela de upload; a **NF-e própria (PRDs 127–129) foi deferida para pós-go-live** (emissão fiscal segue no DINTEC). **Gestão de chaves in-platform (PR #63, v0.85.0 `Keyring`):** tela Owner-only `Configurações → Integrações → Chaves & API` gerencia chaves de API (Resend, WhatsApp por conta, webhook app-level) **criptografadas no Supabase Vault** — wrappers SECURITY DEFINER service_role-only, Edge Function `integration-secrets` (a 10ª) auditada e write-only; runtime resolve **Vault-first com fallback env** (`_shared/secrets.ts` em `whatsapp-send`/`whatsapp-webhook`/`invite-seller-email` — rotação sem redeploy); docs em `docs/dev/integration-secrets.md`. **Ativação DR & Observabilidade COMPLETA (2026-06-10, issues #45 e #52 FECHADAS):** secrets de CI/backup no GitHub e os 3 workflows verdes em execução real (RLS a cada PR; backups semanais dom 03:00 BRT; senha do banco resetada p/ alfanumérica após quebra de parse na URI), Sentry ativo (frontend via `VITE_SENTRY_DSN` na Vercel + edge via secret `SENTRY_DSN`, DSN validado), uptime monitor (UptimeRobot 5/5min no `/functions/v1/health` — a edge `health` v5 aceita `HEAD`, monitores sondam com HEAD); **PITR habilitado no go-live (2026-06-10, junto com o flip #47** conforme decisão do dono; RPO ~2 min — falta apenas o 1º teste real de restauração, §D1). **Switch de ambiente in-platform (PR #64, v0.86.0 `Lever`):** tela Owner-only `Configurações → Avançado → Ambiente & Dados` alterna **Demonstração ↔ Produção em runtime** via override por navegador em `localStorage` (`gallo-data-source-override`/`gallo-auth-source-override`; resolução override > env > mock em `src/shared/lib/environmentMode.ts`, testada) — `factory.ts`/`authSource.ts` leem o override no boot e aplicar **recarrega a página** (troca de auth limpa o espelho de sessão = logout implícito); UI com presets + 2 dimensões avançadas, `DemoModeBanner` global enquanto dados = mock, badge no TopBar e na tela de saúde; **não é fronteira de segurança** (RLS/Auth seguem protegendo; docs em `docs/dev/environment-mode.md`). **WhatsApp multi-instância (v0.97.0 `Switchboard`, mergeada na `main`):** a plataforma deixou de ficar limitada a uma instância por provedor — agora conecta **múltiplos números Evolution** com `AddInstanceWizard` (criação + pareamento QR), **acesso por número** resolvido por atendente/papel/loja inteira + participantes co-responsáveis (`resolveAccessRecipients`), **filtro da Inbox por instância**, **busca de conversas server-side** (RPC `search_conversations`), identidade visual por origem (cor/dot + faixa "Respondendo por" com telefone), conversa nova outbound com escolha do número e `syncFullHistory` por padrão; RLS de conversations/messages delegada a `can_access_conversation`, índices parciais únicos em `instanceName`/`phoneNumberId`, webhook resolve conta por match exato (multi-instância safe). **Épico Pessoas & Acesso — PRD-211 (v0.98.0 `Steward`):** primeira entrega da cadeia 211→212→213. Papéis deixaram de ser estáticos: **Editor de Papéis** Owner-only (`/app/configuracoes/papeis`) edita a matriz de permissões por recurso (CRUD + escopo), com papel "Dono" imutável, papéis de sistema avisados e CRUD de papéis customizados (criar/duplicar/renomear/excluir com guarda de uso); recursos viraram **dado persistido** (tabelas `roles`/`role_permissions`/`rbac_resources`), **Departamentos** (`ITeam` revivido, `/app/configuracoes/departamentos`, membros via `sellers.department_id`, scope `team` resolvido por departamento) e a **tela de Usuários estendida** (Sheet+abas Geral/Horário🔒/Rodízio🔒, campo Departamento). **Enforcement via `base_role`**: a verdade vive nas tabelas, a UI lê por cache em memória (`rbacConfig`, hidratado por `<RbacHydrator/>`, assinatura síncrona de `hasPermission` preservada com fallback estático idêntico) e a RLS continua governada pelo papel base no JWT — todo papel customizado carrega um `base_role`, nenhuma policy reescrita; recursos `manage_roles`/`monitor` adicionados (gate de edição do editor = `manage_roles`). 4 migrations aplicadas em prod. **Épico Pessoas & Acesso — PRD-212 (v0.99.0 `Shift`):** segunda entrega da cadeia 211→212→213. Cada usuário pode ter um **horário de atendimento** (`ISeller.workSchedule` + `scheduleOverrides`) que governa o acesso à plataforma — feature `src/features/access/` com engines puros testados (`workSchedule.ts`: timezone São Paulo por offset fixo **−03:00**, Brasil sem DST desde 2019; `accessGate.ts`: `evaluateAccess` assimétrico — suspenso > isento Owner/Gestor > sem-agenda > grant > janela; `OPERATIONAL_ROLES`=[Vendedor, VendedorExterno, SDR, Financeiro]). **Gate de login client-side** na rota de login (`useAccessGate`, pois a camada auth não pode importar `@/mocks`/factory; sessão restaurada NÃO é bloqueada, só o login explícito; `AccessBlockedNotice` com o próximo horário). Banner de sessão + auto-offline (`OutsideHoursBanner`/`useOutsideHoursWatcher` no `AppLayout` — marca offline 1x, nunca força online; rótulo "fora do horário" na disponibilidade). Override de emergência (`GrantAccessDialog` Owner — liberar por N horas/até HH:mm, auditado, revogável). Editor na aba **"Horário"** do cadastro de usuário (`WorkScheduleTab` **controlado** — agenda semanal + exceções salvas pelo botão único "Salvar alterações" do form, com validação que bloqueia e pula para a aba). **Enforcement SERVER-SIDE (RF-020) DEFERIDO por decisão do dono** — login é 100% client-side via `supabase.auth` (sem Edge Function de login); gate server-side exigiria Auth Hook em prod com risco de trancar acesso. O gate client-side **NÃO é fronteira de segurança** (RLS/`base_role` seguem governando). 1 migration aplicada em prod (3 colunas jsonb `work_schedule`/`schedule_overrides`/`access_grant` em `sellers`). Docs: `docs/dev/work-schedule-access.md`. **Área de Inteligência artificial (v0.100.0 `Synapse`, PR #101):** nova área Owner-only `Configurações → Inteligência artificial` (hub com abas Visão geral · Provedores & chaves · Funcionalidades · Playground), entregue mock-first. Provider `ai` (o **37º**) no Provider Pattern (mock determinístico + stub supabase `NotImplementedError`); 4 engines puros testados em `src/features/ai-settings/engine/` (`aiPricing` USD→BRL, `aiUsage` agregação/KPIs, `aiBudget` projeção run-rate, `aiRouting` modelo efetivo por funcionalidade + fallback); chaves de provedor no Vault (grupo "Provedores LLM" em `integrationKeys`: ANTHROPIC/OPENAI/OPENROUTER/GOOGLE_AI_API_KEY); roteamento de modelo por funcionalidade (copiloto de conversa/analítico, SDR, identificação de peça, insights). Docs: spec/plano/handoff em `docs/superpowers/`. **Integração real de LLM — Sub-projeto 1 (v0.102.0 `Cortex`):** gate demo-only removido — a área aparece para o Owner em produção (o cutover de produção — aplicar a migration e fazer deploy do Edge `ai-generate` — é passo de rollout pendente; ver `docs/dev/ai-llm-integration.md`, "Ordem de deploy"). Edge Function `ai-generate` (**a 11ª**) — proxy Owner-only que faz a chamada real ao provedor (Anthropic ou OpenRouter) com a chave do Vault, aplica teto de orçamento best-effort (`SUM(cost_brl)` em `ai_usage_events`), AbortSignal de timeout (~60 s) e grava o evento de uso. Tabelas `ai_settings` (singleton global, RLS Owner-only; chaves **não** vivem aqui — ficam no Vault) e `ai_usage_events` (append-only, INSERT exclusivo pelo service_role/Edge, Owner lê). `supabaseAiProvider` real substitui o stub: CRUD de settings via cliente direto (RLS Owner) + Playground/teste de conexão via Edge. Catálogo de modelos/preços extraído para `src/providers/data/engine/aiCatalog.ts` (fora de `mock/`); `IAiUsageEvent` ganhou `source`/`feature?`. Teto best-effort **não é atômico** no v1 (TOCTOU conhecida — endurecer antes de plugar consumidores automáticos). **Os consumidores reais (copiloto/SDR/identificação de peça/insights) seguem deferidos** — cada um será um sub-projeto próprio. Docs: `docs/dev/ai-llm-integration.md`. **Épico Pessoas & Acesso — PRD-213 (v0.101.0 `Carousel`):** terceira e última entrega da cadeia 211→212→213. **Fila de rodízio de atendimento por loja** — feature `src/features/rotation/` com engines puros testados: `eligibility.ts` (reusa `isWithinWorkSchedule` do 212), `selectNextFromRotation.ts` (determinístico, sem `Math.random()`, modos `direct`/`department` com pulo de offline + ponteiros independentes e wrap-around) e `applyRotationOverride.ts` (contrato de fronteira com PRD-013). `targetMode` (`direct`|`department`) vive **na própria fila** (`IRotationQueue`, 1 por loja; não duplicado em settings); participantes `IRotationParticipant` com escopo topo/interno; `ISeller.rotation={enabled}`. **Integração num ponto único** — `conversations.create()` (mock+supabase) consulta a fila **após** `distributeConversation`: carteira/especialidade a montante (precedência), a fila como fonte do revezamento (substitui round-robin/carga/fallback_fila), fallback do 013 a jusante; **uma atribuição por conversa**; trace reaproveita `IDistributionTrace` (`criterionMatched='round_robin'`, sem novo enum). **O webhook real (`whatsapp-webhook`) NÃO é tocado** — em prod a conversa nova recebe `customer.sellerId` direto; ativar a fila no webhook exige presença online server-side e fica **DEFERIDO** (espelha o server-side do 212). Tela Owner/Gestor `/app/configuracoes/rodizio` (gate `seller/edit/store`) com **drag-and-drop** (`@dnd-kit`, teclado-acessível), liga/desliga, add/remove, navegação de dois níveis no modo department e **visão ao vivo** ("Agora": próximo + pulados rodando o engine em runtime); aba **"Rodízio"** destravada no cadastro (controlada, salva no botão único do form); link cruzado a partir da tela de Distribuição. Providers `rotationQueues`/`rotationParticipants` (mock-first + supabase, RLS espelhando `departments`); migration `supabase/migrations/20260616180000_rotation_queues.sql` (2 tabelas) **VERSIONADA e APLICADA em prod (2026-06-17)**. Incidentalmente o `bun.lock` foi reconciliado ao `package.json` que já declarava `vite ^8.0.16` (lock estava stale em 7.3.3) — agora `@dnd-kit` + vite 8/rolldown. Docs: `docs/dev/rotation-queue.md`. **Modelo de acesso a conversas reescrito ("2 portões") — v0.110.0 `Turnstile` (PRs #131–#135, 2026-06-20):** a leitura para papéis não-staff resolve em dois portões — **Atendimento** por **instância** (`can_access_conversation`, a instância é o portão-mestre: perdeu o número, perde os atribuídos daquele número) e **Carteira** por **dono** (`customers.seller_id`). Toda leitura escopada por conversa (mensagens, ficha, contato, copiloto, agendador) passa por RPC `SECURITY DEFINER` gated-once — mata o 406 da ficha do pool e o 500/statement_timeout das mensagens (a RLS por-linha não escalava). Parâmetro por loja `participantCrossInstance` (co-responsável entre instâncias) + toggle Owner-only em Configurações → WhatsApp. Junto: "Lead anônimo" da fila corrigido (#131), menu lateral por permissões (#129), `audit_logs.actor_id` correto nas Edge Functions (#130). **Doc: `docs/dev/conversation-access-model.md`** (regra de negócio, padrão de performance e lições). **Performance de mídia das conversas — v0.111.0 `Aperture` (PR #137, 2026-06-21):** a mídia recebida do WhatsApp sofria a **mesma** assimetria de RLS por-linha que afligia as mensagens, mas no caminho do **storage**: a policy `storage_whatsapp_media_select_inbound` fazia `IN (subquery sobre conversations)` — `can_access_conversation` por linha numa varredura de ~837 conversas **a cada `createSignedUrl`** (não-staff ~2.375 ms vs owner ~113 ms por arquivo). Como o path já carrega o `convId`, a policy passou a chamar o helper `SECURITY DEFINER` `can_read_conversation_media` (cast guardado + `can_access_conversation` **uma vez**) → **2.375 ms → ~7 ms**, semântica idêntica (zero alargamento, e2e RLS verde, migration `20260620160000` **aplicada em prod**). No frontend, a assinatura virou **lote** (`partitionMediaRefs` puro + `IMessagesProvider.resolveMediaUrls` via `createSignedUrls` + hook `useSeedSignedMediaUrls` semeando o cache `["message-media-url", ref]` que o `useResolvedMediaUrl` já lê — balões inalterados, fim do N+1), no thread e na galeria "Mídias". Spec/plano em `docs/superpowers/`. Doc mestre de pendências: `docs/fase2-pendencias.md`. Projeto mantido pela AILA Sistemas Inteligentes.

> ⚠️ **Worktrees — IGNORAR.** A pasta `.claude/worktrees/` (qualquer caminho contendo `worktrees`) contém git worktrees isoladas de outras branches e **não faz parte da branch `main`**. Ao explorar, buscar (grep/glob), editar ou raciocinar sobre o código, **ignore completamente** esse diretório. Trabalhe apenas no diretório principal do projeto (sobretudo `src/`). Não relate, edite nem referencie arquivos dentro de `worktrees`.

## Comandos

```bash
bun install          # instalar dependências (bun.lock presente)
bun run dev          # dev server (Vite + TanStack Router)
bun run build        # build de produção (Vite/esbuild)
bun run build:dev    # build em modo development
bun run preview      # preview do build
bun run lint         # ESLint
bun run format       # Prettier --write
bun run test         # Vitest (run único)
bun run test:watch   # Vitest em watch
bun run test:ui      # Vitest UI
```

- **Suite de testes:** **Vitest** (`bun run test`) — arquivos `*.test.ts` co-localizados (~39 arquivos). TDD nos `engine/` de negócio.
- ⚠️ **O `bun run build` (Vite/esbuild) NÃO faz type-check** — transpila sem checar tipos. Para checagem de tipos rode `bunx tsc --noEmit` à parte. Há um baseline de erros pré-existentes em `tsc`; avalie **código novo por delta** (cruze com `git diff --name-status main...HEAD --diff-filter=A` para isolar arquivos criados na branch). O gate prático de CI é `bun run build` + `bun run test`.
- Os scripts `pre*` (`predev`, `prebuild`, `prebuild:dev`, `prepreview`) rodam `node scripts/copy-changelog.mjs`, que copia o `CHANGELOG.md` para `public/` para consumo em runtime (página "Sobre/Novidades"). Rodar Vite sem o pre-hook deixa o changelog em runtime desatualizado.

## Stack e arquitetura

- **Modelo de deploy:** SPA estática hospedada na Vercel. `index.html` na raiz é o template Vite; `vercel.json` faz rewrite de todas as rotas para `/index.html` (padrão SPA). Sem SSR.
- **Entry:** `src/main.tsx` faz `createRoot` em `#root` e renderiza `<RouterProvider>`.
- **Vite config:** plugins explícitos em `vite.config.ts` — `tanstackRouter` (com `autoCodeSplitting`), `react`, `tailwindcss`, `tsconfigPaths`. Injeta constantes de build via `define`: `__GIT_BRANCH__` e `__APP_VERSION__` (usadas no footer dev-only e como fallback de versão). Sem framework wrapper opinionado por trás.
- **Router:** TanStack Router file-based (**sem** TanStack Start). Rotas em `src/routes/`; `routeTree.gen.ts` é **gerado** pelo plugin (não editar). Root route em `src/routes/__root.tsx`. Meta tags, Google Fonts, favicons e script anti-FOUC vivem no `index.html`.
- **Árvore de providers** (em `__root.tsx`, de fora para dentro): `QueryClientProvider` → `ThemeProvider` → `CopilotSettingsProvider` → `DataProvidersProvider` → `NotificationProvidersProvider` → `AuthProvider` → `MultistoreProvider` → `<Outlet>`.
- **React 19.**
- **UI:** Tailwind CSS **v4** + shadcn/ui (style `new-york`, baseColor `slate`) em `src/components/ui/`. Iconify (`@iconify/react`) é o ícone padrão; wrapper em `src/components/Icon.tsx` (`lucide-react` também disponível). Gráficos com **recharts**. Outras libs de UX: `cmdk` (command palette), `embla-carousel-react`, `vaul` (drawer), `react-day-picker`, `react-resizable-panels`, `three` (3D pontual).
- **Estado servidor:** TanStack Query — `QueryClient` criado em `src/router.tsx` e injetado via router context.
- **Estado cliente:** **Zustand** para estado leve em memória, sobretudo o store da camada de mocks. Sem Redux.
- **Formulários:** react-hook-form + zod + `@hookform/resolvers`.
- **Toasts:** sonner.
- **Path alias:** `@/*` → `src/*` (definido em `tsconfig.json` e `components.json`).

## Estrutura de pastas (arquitetura-alvo, já materializada)

```
src/
├── features/<feature>/   # arquitetura feature-driven ATIVA (~50 features)
│                         # ex.: customers, vehicles, leads, orders, quotes, catalog,
│                         #      sales-analytics, goals, commissions, expenses, cashflow,
│                         #      dre, conversations, sdr, media, quick-send, b2b-portal,
│                         #      storefront*, rbac, external-seller-pwa, multistore,
│                         #      shell (layout), about, ...
│                         # Cada feature pode ter: components/, hooks/, pages/, utils/,
│                         #      engine/ (lógica de negócio), api/, store/, i18n/, index.ts (barrel)
├── routes/               # TanStack Router file-based (~120 arquivos)
│                         # prefixos: app.* (SaaS logado), app.gestao.*, app.configuracoes.*,
│                         #           auth.* (login), loja.* (storefront B2C),
│                         #           portal.* (portal B2B), pwa.* (app do vendedor externo)
├── providers/data/       # Provider Pattern (camada de dados — ver abaixo)
│   ├── contracts/        # interfaces por domínio (IXxxProvider) + IDataProviders
│   ├── impl/mock/        # implementações mock (Fase 1)
│   ├── impl/supabase/    # implementações Supabase (Fase 2 — 37 providers reais; cutover por env)
│   ├── hooks/            # useXxxProvider() — acesso via context
│   ├── factory.ts        # getDataProviders() seleciona o set por VITE_DATA_SOURCE
│   ├── context.tsx       # DataProvidersProvider (React context)
│   ├── errors.ts         # NotImplementedError, etc.
│   └── auditLogger.ts    # trilha de auditoria
├── mocks/                # camada de dados fictícios (Fase 1) — privada (ver ESLint)
│   ├── api/              # endpoints simulados (getCustomers, createOrder, ...)
│   ├── data/             # seeds estáticos (catálogo de peças, papéis, lojas, ...)
│   ├── generators/       # factories faker + seed determinístico (bootstrap)
│   ├── store/            # Zustand in-memory (mockStore)
│   ├── hooks/            # hooks internos da camada mock
│   ├── config.ts         # VOLUMES, seed default, latência, taxa de erro
│   └── index.ts          # ÚNICO barrel público da camada mock
├── shared/
│   ├── types/            # modelo de domínio (interfaces I*, ~30 arquivos) + barrel index.ts
│   ├── hooks/            # hooks compartilhados (ex.: usePersistedListSearch)
│   └── utils/            # contrast.ts, utils.ts
├── components/
│   ├── ui/               # shadcn/ui
│   ├── layout/           # wrappers de layout
│   ├── Icon.tsx          # wrapper Iconify
│   ├── Logo.tsx, ThemeSwitcher.tsx
│   └── ThemeProvider.tsx # context de tema (2 dimensões)
├── config/themes.ts      # tokens e metadados de tema, LOCALSTORAGE_KEYS
├── hooks/                # legado: useTheme.ts, use-mobile.tsx
├── lib/                  # legado: contrast.ts, utils.ts
└── main.tsx, router.tsx  # entry e setup do router
```

> A estrutura legada (`src/components`, `src/hooks`, `src/lib`, `src/config`) coexiste mas está em fim de vida — código novo vai em `features/`, `shared/`, `providers/` e `mocks/`. Atenção: o hook de tema ainda vive em `src/hooks/useTheme.ts` (não em `shared/hooks`).

## Camada de dados, Provider Pattern e mocks

- **Provider Pattern (PRD-005):** features **nunca** consomem `src/mocks/` diretamente. Acessam dados via `useXxxProvider()` hooks de `@/providers/data`. O `factory.ts` monta `mockProviders`/`supabaseProviders` (37 providers: customers, vehicles, leads, conversations, messages, parts, quotes, orders, commissions, expenses, cashflow, goals, recommendations, transfers, segments, sellers, stores, settings, audits, whatsappAccounts, distributionTraces, managerDashboard, sdrSessions, sdrEscalations, copilot, indicators, vehicleModels, modelKits, media, assetLibrary, quickReply, trackableLink, scheduledSend, storefront, systemHealth, messageTemplates, ai).
- **Fronteiras impostas por ESLint** (`eslint.config.js`, `no-restricted-imports`): fora de `src/mocks/**` e `src/providers/data/**`, são **proibidos**: import de `@/mocks` / `@/mocks/api/*` (use providers), de módulos internos do mock (`store`/`generators`/`data`/`config`), de `@/providers/data/impl/*`, de `@/providers/data/contracts/*` individuais e de `@/providers/data/factory`. Tudo passa pelo barrel `@/providers/data`. Exceção: `src/routes/design-system.tsx` (usa utilitário de reset de seed).
- **Switch de fonte de dados:** `getDataProviders()` resolve `VITE_DATA_SOURCE=mock|supabase` (default `mock`; valor inválido cai em `mock` com warning em dev). Build-time only — sem troca em runtime. Documentado em `.env.example` e `src/vite-env.d.ts`.
- **Dados determinísticos:** base mock gerada por seed (`seedrandom` + `@faker-js/faker`) — mesmo dataset entre reloads. Volumes/seed em `src/mocks/config.ts`.
- **Auditoria:** mutações registram trilha via `auditLogger` (`recordAuditLog` async / `recordAuditLogSync`).

## Modelo de domínio

Tipos em `src/shared/types/` (barrel em `index.ts`). Interfaces de domínio prefixadas com **`I`**. Principais agrupamentos:

- **platform.ts** — `IStore`, `IPlatformSettings`, `IPipelineStage`, `ILossReason`, `IGamificationRules`, ...
- **people.ts** — `ISeller`, `IRole`, `IPermission`, `IAuditLog`, `ICommissionRule`
- **customer.ts** — `ICustomer` (B2B/B2C), `ICustomerNote`, `ICustomerSegment`
- **lead.ts** — `ILead`, `ILeadStage`, `ICarteiraTransfer`
- **conversation.ts** — `IConversation`, `IMessage`, `IWhatsAppAccount`
- **commercial.ts** — `IOrder`, `IOrderItem`, `IQuote`, `ICommission`
- **catalog.ts** — `IPart`, `IApplication`
- **media.ts / quickSend.ts** — gestão de mídia (PRD-026) e envio rápido/biblioteca de ativos (PRD-027): `IMediaAsset`, `IAssetLibraryItem`, `IQuickReply`, `ITrackableLink`, `IAssetCombo`, `IScheduledSend`
- **dre.ts / expenses.ts / cashflow.ts / inventory.ts / inventory-movement.ts / bi.ts** — financeiro e BI
- **storefront.ts / portal-b2b.ts / visit.ts / distribution.ts / shipping.ts / sdr*.ts / insights.ts** — demais domínios
- **common.ts** — utilitários (`ID`, `ISO8601`, `Money`, `Division`)

Padrões transversais do modelo:

- **Multi-loja desde o modelo** — `IStore` é entidade de primeira classe; toda entidade comercial carrega `storeId`. Seleção de loja via `MultistoreProvider`.
- **Campo `division: 'parts' | 'service' | 'industrial'`** em entidades comerciais — default `parts` no MVP; SERVICE e INDUSTRIAL ficam dormentes mas modeladas.

## Sistema de temas (PRD-001)

Modelagem em **duas dimensões CSS independentes** no `<html>`:

```html
<html data-theme="diesel|parts|service|industrial" data-mode="light|dark"></html>
```

Mais a classe `.dark` (variante Tailwind via `@custom-variant dark (&:is(.dark *))`).

- **Persistência:** `localStorage` chaves `gallo-theme` e `gallo-mode` — constantes em `src/config/themes.ts` (`LOCALSTORAGE_KEYS`). `mode` aceita `auto` (observa `prefers-color-scheme`).
- **Anti-FOUC:** script inline no `<head>` do `index.html` aplica atributos antes do primeiro paint. Se mudar as chaves do localStorage ou os nomes de tema, **atualizar esse script também**.
- **Provider/hook:** `ThemeProvider` em `src/components/ThemeProvider.tsx`, hook em `src/hooks/useTheme.ts`. Acesso de leitura sempre via `useTheme()`.
- **Tokens em 3 camadas** (`src/styles.css`):
  1. **Primitivos** (`:root` — `--gallo-*`): paleta GALLO bruta. Não usar direto em componentes.
  2. **Semânticos** (`@theme inline`): `--background`, `--foreground`, `--primary`, etc. — mapeados para Tailwind/shadcn.
  3. **Tema** (`[data-theme="…"]` + `.dark|.light`): reescreve semânticos.
  - **Componentes consomem APENAS tokens semânticos** (`bg-background`, `text-foreground`, `border-border`…). Nunca referenciar `--gallo-*` ou hex direto. Severidades via utilitários Tailwind `text-/bg-/border-severity-{info|success|warning|critical}`.
- **Rota `/design-system`:** página de visualização de tokens, tipografia, componentes shadcn e validador de contraste WCAG. É **dev-only** (`beforeLoad` chama `redirect({ to: '/' })` quando `!import.meta.env.DEV`).

## Convenções de código

- **camelCase** variáveis/funções, **PascalCase** componentes/tipos, **kebab-case** arquivos, **snake_case** colunas DB (Fase 2), **UPPER_SNAKE_CASE** constantes globais.
- **Comentários:** inglês. **UI/conteúdo de usuário:** português do Brasil com acentos corretos (UTF-8 — nunca `nao`/`avaliacao`/`conclusao`).
- **Commits:** Conventional Commits em inglês (`feat:`, `fix:`, `refactor:`, etc.), atômicos.
- **TypeScript:** `strict: true`. Evitar `any`. Interfaces de domínio prefixadas com `I`.
- **Regras de UX (obrigatório em telas novas/reformadas):** seguir `docs/dev/ux-guidelines.md` — header glassmorphism com tokens semânticos, linha de progresso de scroll na divisa do bloco fixo (`ScrollProgressBar`), busca com largura dinâmica + atalho `/` + `kbd` + `Escape`, tabelas com colunas redimensionáveis (`@/shared/hooks/useResizableColumns`, persistência `gallo-<feature>-column-widths`), delimitadores verticais **somente no header** e menu de visibilidade de colunas no **clique-direito do cabeçalho** (`ContextMenu` "Colunas visíveis" + "Exibir todas").
- `bunfig.toml` impõe **24h supply-chain guard** (`minimumReleaseAge = 86400`). Antes de adicionar pacote à `minimumReleaseAgeExcludes`, **confirmar com o usuário**.

## Fluxo de desenvolvimento dirigido por PRD

Toda implementação substantiva é guiada por um PRD em `docs/prds/`. Documento mestre: `docs/prds/briefing-execucao-prds.md` — modelo conceitual completo (50+ entidades), índice dos PRDs, decisões arquiteturais transversais e estrutura de pastas alvo. Índices/roadmaps adicionais (`INDEX-PRDs-*`, `ROADMAP-FASE2-*`) também vivem nessa pasta.

- ~76 arquivos de PRD em `docs/prds/`; PRDs concluídos são marcados com sufixo **`_DONE`** no nome do arquivo — cobrem Design System, Shell, Provider Pattern, RBAC, multistore, clientes, veículos, leads, orçamentos, pedidos, catálogo, analytics de vendas, ranking/gamificação, positivação, curva ABC, carteira analítica, metas, comissões, DRE, rentabilidade, despesas, fluxo de caixa, estoque, atendimento, SDR, insights, gestão de mídia (PRD-026), envio rápido/biblioteca de ativos (PRD-027), storefront B2C completo (home/busca/categoria/ficha/carrinho/conta/admin), portal B2B e PWA do vendedor externo.
- Faixa **100+** (infra Supabase / Fase 2): **`PRD-100` a `PRD-110` estão `_DONE`** (100–108 em 2026-06-09; 109/110 em 2026-06-10, PR #51), cada um com ressalvas anotadas no cabeçalho do próprio arquivo (CI gated em secrets #45, ativação DR/observabilidade #52, itens deferidos com motivo). Destaques entregues: migrations versionadas em `supabase/migrations/` (espelho do remoto — manter sincronizado a cada `apply_migration`), `_shared/` + canário nas Edge Functions, Realtime core no Inbox (gate por `VITE_DATA_SOURCE`), Storage com upload real de mídia, MVs de BI com refresh `pg_cron` lidas via RPCs scoped, workflows de backup + runbooks de DR (`docs/infra/`), Sentry gated, healthcheck público e dashboard de saúde Owner-only (`docs/ops/observability.md`). **Onda 5 (WhatsApp real) COMPLETA:** `PRD-111`–`PRD-120` estão `_DONE` (2026-06-10; PR #53 = v0.76.0 `Bridge` para 111–113; PR #54 = v0.77.0 `Gateway` para o webhook unificado `whatsapp-webhook`; PR #55 = v0.78.0 `Courier` para o pipeline de envio `whatsapp-send` + hook `useMessageSend`; PR #56 = v0.79.0 `Stencil` para o catálogo de templates HSM — tabela `message_templates`, provider `messageTemplates`, render engine, tela de config e `TemplatePicker` (docs: `docs/dev/whatsapp-templates.md`); PR #57 = v0.80.0 `Hourglass` para a janela de 24h — engine puro `engine/sessionWindow.ts`, RPC `public.last_inbound_at` (SECURITY INVOKER, authenticated-only) via `IMessagesProvider.getLastInboundAt`, CTA de template no banner (docs: `docs/dev/whatsapp-session-window.md`); PR #58 = v0.81.0 `Pulse` para o status tracking — lifecycle `queued→sent→delivered→read|failed` com anti-regressão, Realtime na conversa (`useRealtimeMessages`), retry real (`retryOfMessageId`, audit `dispatch_retry`), gate de número inválido (`customers.whatsapp_status`, Meta `131026`, override staff auditado, revalidação manual) e saúde de entrega via RPC owner-only `whatsapp_delivery_health` na tela `/app/gestao/saude` (docs: `docs/dev/whatsapp-status-tracking.md`); PR #59 = v0.82.0 `Weave` para a reconciliação com a Fase 1 (PRD-119) — anexo de arquivo avulso real no composer (`useAttachmentUpload`: upload PRD-026 + envio PRD-115; mock = object URL local), tela `WhatsAppAccountsPage` (Configurações → WhatsApp, edição staff-only de label/`credentials_ref`/`provider_config`, contrato `update`, migration `20260610145458` aplicada), Simulação SDR confirmada sandbox local by design, `docs/dev/onda5-migration.md` + README mínimo; PR #60 = v0.83.0 `Relay` para failover + monitoring (PRD-120, **fecha a onda**) — colunas de failover em `whatsapp_accounts` (migrations `20260610152000`/`20260610153000` aplicadas), engine puro `failover.ts` (matriz RF-020, auto-activate/restore 30min), conta efetiva no `send/core.ts` (audit `failover_used`; `FAILOVER_INCOMPATIBLE` 422 para template em reserva Evolution), tick SQL `whatsapp_health_tick` via pg_cron */5 (SQL-only — pg_net não habilitado, healthCheck ativo deferido) com audit + notificações in-app, RPC owner-only `whatsapp_provider_health`, override manual auditado na tela de contas (docs: `docs/dev/whatsapp-failover.md`) — núcleos runtime-agnostic em `webhook/core.ts` e `send/core.ts`, espelhados via `scripts/sync-whatsapp-shared.ts` — ⚠️ regra: mudou `src/providers/whatsapp/` ⇒ rodar o sync e redeployar) — camada `src/providers/whatsapp/` **runtime-agnostic** (só Web APIs + imports relativos, deps injetadas `resolveSecret`/`logIntegration`) com interface, mock, factory por conta e os engines reais `MetaCloudProvider` (Graph v20.0 pinada, HMAC constant-time) e `EvolutionProvider` (apikey, mídia por URL); segredos como secrets de Edge Function nomeados pelo prefixo `credentials_ref` (nunca no banco), auditoria em `public.integration_logs` (owner-only read). Engines reais rodam server-side — os PRDs 114/115 espelham a camada em `_shared/whatsapp/`; no app, mock + `getEngineCapabilities()` (docs: `docs/dev/whatsapp-providers.md` + guias por engine). **Onda 6 (DINTEC) ENCERRADA POR DECISÃO (2026-06-10):** `PRD-121` está `_DONE` (PR #62 = v0.84.0 `Anchor` — camada `src/providers/dintec/` com interface `IDintecImportProvider`, tipos normalizados `DintecRow`/`DintecImportBatch`, factory por store com gate `VITE_DINTEC_PROVIDER=mock` e `MockDintecProvider` determinístico; docs: `docs/dev/dintec-providers.md`); os `PRD-122`–`PRD-126` (CSV provider/upload UI, engine `dintec-process`, syncs e reconciliação) foram **DEFERIDOS por decisão do dono** — o DINTEC não disponibiliza export de dados (talvez jamais disponibilize) e as importações via CSV, quando ocorrerem, serão **conduzidas de forma assistida pelo agente desenvolvedor** (dry-run + revisão caso a caso), não por tela de upload. A camada PRD-121 fica como fundação agnóstica de fonte. `PRD-127`–`PRD-129` (NF-e própria) foram **DEFERIDOS PARA PÓS-GO-LIVE por decisão do dono (2026-06-10)** — a emissão fiscal segue no DINTEC; NF-e própria só se torna necessária na substituição do ERP, e o início dependerá de gates do dono (contratar provedor fiscal Focus/PlugNotas/eNotas, certificado A1, NCM real no catálogo; os PRDs 130/131 referenciados não existem na pasta). `PRD-201` é backlog, não iniciado.

Padrões arquiteturais que devem ser preservados ao implementar features:

- **Provider Pattern** — features acessam dados só via `@/providers/data`; switch Mock → Supabase por `VITE_DATA_SOURCE`.
- **Multi-loja** — `IStore` de primeira classe; toda entidade comercial carrega `storeId`.
- **Campo `division`** — `parts` default; `service`/`industrial` modeladas e dormentes.
- **Feature folder** — novo código em `src/features/<feature>/` com barrel `index.ts`; lógica de negócio em `engine/` (testada com Vitest).

## Versionamento e changelog

- **SemVer.** MINOR/MAJOR recebem **codinome em inglês** (atual: `Sonar` — v0.147.0).
- **CHANGELOG.md** segue Keep a Changelog; atualizado no version bump, não a cada commit. É copiado para `public/` pelos pre-scripts (`scripts/copy-changelog.mjs`).
- Bump após PRD completo (obrigatório) ou quando solicitado para acumulado de fixes diretos.
- **Tags git** acompanham cada bump: `vX.Y.Z` no commit de release/merge (ex.: `v0.67.0` Vault, `v0.68.0` Dispatch, `v0.69.0` Beacon, `v0.70.0` Guide, `v0.71.0` Focus, `v0.72.0` Bedrock, `v0.73.0` Keystone, `v0.74.0` Backbone, `v0.75.0` Sentinel, `v0.76.0` Bridge, `v0.77.0` Gateway, `v0.78.0` Courier, `v0.79.0` Stencil, `v0.80.0` Hourglass, `v0.81.0` Pulse, `v0.82.0` Weave, `v0.83.0` Relay, `v0.84.0` Anchor, `v0.85.0` Keyring, `v0.86.0` Lever, `v0.87.0` Socket, `v0.88.0` Chronicle, `v0.89.0` Roster, `v0.89.1` Roster, `v0.90.0` Mosaic, `v0.90.1` Mosaic, `v0.91.0` Portrait, `v0.92.0` Unfold, `v0.93.0` Almanac, `v0.94.0` Recall, `v0.95.0` Nameplate, `v0.96.0` Alias, `v0.97.0` Switchboard, `v0.98.0` Steward, `v0.99.0` Shift, `v0.100.0` Synapse, `v0.101.0` Carousel, `v0.102.0` Cortex, `v0.103.0` Polyglot, `v0.104.0` Manifest, `v0.105.0` Insignia, `v0.106.0` Freight, `v0.107.0` Tandem, `v0.107.1` Tandem, `v0.108.0` Quill, `v0.109.0` Mandate, `v0.110.0` Turnstile, `v0.111.0` Aperture, `v0.112.0` Lexicon, `v0.113.0` Compass, `v0.114.0` Vigil, `v0.115.0` Curator, `v0.116.0` Chorus, `v0.117.0` Custody, `v0.118.0` Lens, `v0.119.0` Cadence, `v0.120.0` Wellspring, `v0.121.0` Conduit, `v0.122.0` Registry, `v0.123.0` Tether, `v0.124.0` Salvage, `v0.124.1` Salvage, `v0.124.2` Salvage, `v0.124.3` Salvage, `v0.125.0` Intake, `v0.126.0` Waypoint, `v0.126.1` Waypoint, `v0.127.0` Vantage, `v0.128.0` Paperclip, `v0.128.1` Paperclip, `v0.129.0` Unison, `v0.130.0` Marker, `v0.131.0` Ledger, `v0.131.1` Ledger, `v0.132.0` Epilogue, `v0.133.0` Herald, `v0.134.0` Ensemble, `v0.134.1` Ensemble, `v0.134.2` Ensemble, `v0.135.0` Ripple, `v0.135.1` Ripple, `v0.135.2` Ripple, `v0.136.0` Satchel, `v0.137.0` Promote, `v0.138.0` Sidecar).

